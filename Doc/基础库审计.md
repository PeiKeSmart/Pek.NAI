# NewLife.AI 基础库全景审计

> 版本：v1.0 | 日期：2026-08-04
> 范围：`NAI/NewLife.AI/` 全部 12 维度逐目录扫描。协议客户端 chat/stream/think 维度已完成 12 轮分析（见 [协议客户端分析](协议客户端分析.md)），本审计重点覆盖其余维度与协议客户端残留。

---

## 1. 概述

本审计对 `NewLife.AI` 基础库做全景逐目录扫描，目标是**确保这个 NewLife AI 生态核心基础库无瑕疵**。发现问题登记到本文档，通过 dev-loop 分轮修复，全部闭合。

**验证基线**：`NAI/NewLife.AI.sln` 构建 0 错误；全量测试 **1495 通过 / 0 失败 / 7 跳过**（跳过为未配置 Key 的集成测试）。基线测试 1466，本审计新增 29 个测试。

> **第 5 轮（2026-08-04）补充**：NewLife.AI 库自身编译警告 110 → **0**（CS8604×62/8602×20/8619×12/8601×4/1574×6/1573×6 全部清除）；Slack/Telegram Markdown 转换方法改 internal 并补 11 个纯逻辑测试。全量测试 1484 → 1495。
>
> **第 6 轮（2026-08-05）补充**：深扫 Tools/Agents/Coding/Clients/Services/Handlers/Models 区域，新登记 A-73~A-94 共 **22 项**（4 P1、15 P2、3 P3），21 项修复 + 1 项暂缓（A-94 协议专用请求消息一致性，需代理列表架构改动）。核心修复：请求级状态重置、SSRF 重定向加固、工具过滤契约、非流式截断、ValueTask 支持、URL 版本去重（v1beta）、响应释放、CTS 竞态、拉模型长超时。补 5 个测试。
>
> **第 7 轮（2026-08-07）补充**：12 维度复审 + 代码级优化，新登记 A-95~A-103 共 **9 项**（1 P1、4 P2、4 P3）全部修复。核心：DashScope 联网意图检测跨请求 Items 污染（P1，行为+费用影响）、MessageRateLimiter 清理触发误判（count%200==1）、AiClientBase HttpClient 惰性初始化并发双创建、6 处 resp.Model 无条件覆盖服务端真实 model（计费归属失真）、TTS 音频下载未池化、Anthropic/Bedrock ToChatRequest 丢 ToolCalls/ReasoningContent/signature、VectorData/ILocalTextEmbedder 注释 v1 残留、6 处 public static JsonOptions 可变、ChatErrorHelper CS8602 编译警告（08-05 新增文件引入）。补 12 个测试（含 3 个新测试文件）；全量测试 **1534 通过 / 0 失败 / 33 跳过**。另做代码级优化：ToolChatClient.CheckContextLimit 工具 schema Token 估算移出循环（消除每轮重复 JSON 序列化）。
>
> **第 8 轮（2026-08-07）补充**：深度扫 ToolRegistry/ToolSchemaBuilder/CodingAgent/McpServer/McpClientService/ChatClientBuilder/AiClientBase 流式/HashTextEmbedder/AIContentHelper/MergeToolCallDelta 等区域，确认均无新问题（MCP 工具列表 30s 缓存、ToolSchemaBuilder 仅注册时构建、流式解析无冗余分配）。新登记 A-104 共 1 项优化并完成；另优化 ToolRegistry.InvokeMethodAsync 对同一 method 重复 `GetParameters()` 反射调用（改为一次获取复用）。
>
> **第 9 轮（2026-08-07）补充**：ToolRegistry 补 A-105（同名工具重复注册静默丢弃无提示 → XTrace 警告日志）并文档化启动注册线程契约（类 remarks）；复核 FilteredChatClient（R11-1 流式聚合/A-69 错误日志在位）、ChatCompletionRequest.BuildBody（无冗余）。全量测试 **1534 通过 / 0 失败 / 33 跳过**。
>
> **第 10 轮（2026-08-07）补充**：深扫 Models 全部 DTO（Transcription/Rerank/ModelList/Balance/EmbeddingSetting/Checkpoint/QuestionGroup/ToolCallDto/ChatTool/ChatModels）、Capabilities 接口（IImage/IVideo 等）、Image/Video/Speech wire DTO、Embedding 实现、CodingTools 沙箱边界（A-13 在位）——均确认干净。测试项目警告清零：NativeToolTests CS8632×6（移除可空禁用上下文下的无意义 `?`）、HttpClientPoolTests xUnit1030（测试内移除 ConfigureAwait）、AzureAIChatClientTests xUnit2018（IsNotType→IsNotAssignableFrom）。**基础库 NewLife.AI 自身 0 警告确认**；定位相邻项目负债（不在本库范围）：NAI/Test 224×CS8632（无 Nullable 设置却用 `?`）、NewLife.ChatAI 可空性警告 + CS0414 死字段。
>
> **第 11 轮（2026-08-07）补充**：**A-94 架构级改动完成**（此前暂缓项）。方案演进：先实现写回代理 `MessageWriteBackList`（5 协议类加 SyncFromMessages），经评估类复杂度增加不划算，**按用户建议回退改用网关统一化方案**——网关入口 `GatewayController.NormalizeGatewayRequest` 将协议专用请求（Anthropic/Gemini/Ollama）经 `ToChatRequest()` 转为统一 ChatRequest，过滤器链工作于统一对象（天然无适配器写回问题）；配套补全 ToChatRequest（Ollama 新建、Anthropic/Gemini 补 UserId/ConversationId/Items/Tools/EnableThinking 等，对称 A-64）。另修 DeepSeekIntegrationTests/InMemoryVectorStoreTests 的 CS8632。补 4 项 ToChatRequest 完整性测试（`ToChatRequestCompletenessTests`）；全量测试 **1538 通过 / 0 失败 / 33 跳过**。
>
> **第 12 轮（2026-08-07）补充**：**A-106 修复**（网关统一化遗留缺陷）：AnthropicRequest/GeminiRequest 的 `IChatRequest.Tools` 为自动属性（`[IgnoreDataMember]`），入站时原生 tools 数组反序列化填充 `IList<Object>`，但 `IChatRequest.Tools`（`IList<ChatTool>`）保持 null——网关统一化 `ToChatRequest()` 后 `Tools = ((IChatRequest)this).Tools` 为 null，**Anthropic/Gemini 入站工具定义静默丢失**（此缺陷 A-94 之前即存在，`CreateGatewayFlowContext` 读取的也是 `request?.Tools`）。对比 Ollama 有 `_chatTools` 惰性转换不受影响。修复：与 Ollama 一致改为惰性转换——原生 Tools 数组（JsonElement/Dictionary 兼容）→ ChatTool 列表（Anthropic 取 name/description/input_schema；Gemini 遍历 functionDeclarations 取 name/description/parameters）。**实现复用 `CollectionHelper.ToDictionary()`**（NewLife.Core 原生支持 JsonElement 递归转换，返回大小写不敏感字典），替代手写 `FullName == "System.Text.Json.JsonElement"` 检测 + `JsonParser.Decode`（避免重复造轮子）。**顺带统一 Ollama 的 `IChatRequest.Tools` 与 `MapResponseFormat`**（同模式手写 JsonElement 检测）；**并发现 A-107**：Gemini/Ollama 的 ResponseFormat 映射对 JsonElement 入站 `ToJson()` 序列化为 `{"ValueKind":1}` 导致结构化输出格式丢失，一并复用 ToDictionary 修复。补 8 项测试（`GatewayToolsRoundTripTests` 含 Ollama 工具入站解析/ToChatRequest 保留；`GeminiResponseFormatJsonElementTests` 验证 JsonElement 响应格式映射）+ 2 项 `ToDictionaryJsonElementTests`；全量测试 **1550 通过 / 0 失败 / 33 跳过**。
>
> **第 13 轮（2026-08-07）补充**：**A-71 低成本闭合**（此前暂缓项）：`DashScopeRealtimeClient` 单条 WS 消息可能包含多个 JSON 事件（服务端批量推送拼接），整条 `JsonParser.Decode` 因多顶层对象解析失败 → 事件全部静默丢弃。暂缓理由曾是"需精细 JSON 流解析"，现用低成本方案：新增 `SplitJsonObjects` 按顶层大括号配对切分（正确处理字符串值内大括号与转义引号），逐片段 `RealtimeEvent.Parse`；尾部残缺片段保持丢弃语义（与原行为一致），不引入复杂错误路径。补 6 项测试（新增 `DashScopeRealtimeSplitTests`：单事件原样/多事件切分/字符串内大括号/转义引号/逐片段Parse/空文本）；全量测试 **1556 通过 / 0 失败 / 33 跳过**。其余暂缓项（A-03 同步阻塞安全做法、A-31 MCP 同步 API 架构权衡、A-41 扩展点保留）均为权衡决策，维持暂缓。
>
> **第 13 轮续（2026-08-07）补充**：**A-108 修复**（排查 TTS WebSocket 路径发现的同类缺陷）：验证确认 `JsonParser.Decode` 对多顶层对象**静默返回第一个**（非 null）——`DashScopeChatClient.ReceiveWebSocketJsonAsync`（Qwen-TTS-Realtime / CosyVoice 实时合成）单条消息含多事件时后续事件丢失，可能致 `session.finish` 不发送、接收循环卡住。修复：复用 `SplitJsonObjects` 新增 `ParseJsonEvents` 静态方法切分多事件；`ReceiveWebSocketJsonAsync` 单事件直接返回、多事件入按 ws 隔离的 `_pendingEvents` 队列（ConcurrentDictionary）逐次消费；两个 TTS 接收循环 finally 清理队列条目防泄漏。补 6 项测试（新增 `DashScopeTtsEventParseTests`）；**并验证 `RealtimeEvent.Parse` 健壮性**：`JsonParser.Decode` 返回 `NullableDictionary`，缺失键直接索引返回 null 不抛异常（type/event_id 必选键、session/response/delta 可选键的注释成立），补 2 项 Parse 健壮性测试（缺失 type / 缺失可选键不中断接收循环）。全量测试 **1564 通过 / 0 失败 / 33 跳过**。
>
> **第 14 轮（2026-08-10）补充**：公共 API 表面审计 + 测试覆盖补强 + 性能基准与优化。
> - **A-109（性能）**：`HashTextEmbedder.Embed` 长文本分配热点（基准实测 200 句/7489 字分配 896.6 KB、Gen2 124.5 次高频回收）。根因：① Tokenize 物化 ~12600 个 token 字符串（~500 KB）；② tf 字典字符串键（~200 KB）；③ `Murmur128.ComputeHash` 每次调用分配 ~144 B（微基准实测，复用实例亦如此），且 `TryComputeHash` 抛 `The algorithm's implementation is incorrect`（NewLife.Core 缺陷，A-110）。优化为**混合去重哈希**：CJK unigram/bigram 用 Int64 打包键（`char` / `(char<<16)|char`）去重替代字符串键、英文单词保留 String 键（`ToLowerInvariant` 多字符展开语义）、复用单个 Murmur128 实例 + 8 字节 UTF8 缓冲、仅对唯一 token 哈希（调用数 ~12600→~2000）。效果：Embed_Long **459.5→152.8 µs（↓67%）**、**896.6→62.9 KB（↓93%）**、Gen2 124.5→0.24（消除）；Embed_Short 3.225→2.926 µs、8.13→6.3 KB。补 2 项黄金值测试（精确非零桶断言，锁定语义逐位一致）。**后续（内联零分配，见 A-110）再降**：Embed_Long **152.8→138.9 µs**、**62.9→22.85 KB（↓64%）**；Embed_Short **2.926→1.90 µs**、**6.3→3.77 KB（↓40%）**，逐 token 哈希分配归零。
> - **A-110（已修复）**：`Murmur128.TryComputeHash` 抛 `InvalidOperationException: The algorithm's implementation is incorrect`。根因：`Murmur128` 仅重写 `HashSize` 属性、从未设置基类受保护字段 `HashSizeValue`（默认 0），基类 `TryHashFinal` 校验 `final.Length(16) == HashSizeValue/8(0)` 失败即抛错（.NET 8/10 源码实证）。**已在 NewLife.Core 修复**：构造器设置 `HashSizeValue = 128`（权威修复，全 TFM 生效）+ `#if NET5_0_OR_GREATER` 补零分配 Span 路径（`HashCore(ReadOnlySpan)`/`TryHashFinal(Span)`），新增 7 项测试（TryComputeHash 与 ComputeHash 一致性、黄金值锁定双路径）。但 NAI 目标框架 net45/netstandard2.0/netstandard2.1 **无法编译 .NET 5+ 的 `TryComputeHash`**（netstandard2.1 引用程序集无该 API，实证确认），故 NAI 侧改用**内联零分配 Murmur128**（`ComputeMurmur128`，seed=0 逐位复刻写入预分配缓冲区，黄金值测试锁定输出逐位一致），彻底消除逐 token 哈希分配（见 A-109 效果）。
> - **公共 API 清理**：删除 `Agents/FunctionCallingPlanner.cs`（含 `PlanStep`/`PlanStepStatus`/`PlanResult`/`FunctionCallingPlanner` 4 个公共类型——全生态零消费者、零测试，与 `Planner.FunctionCallingPlanner(: IPlanner)` 功能重复的孤儿 API）。
> - **巨型文件拆分**：`DashScopeChatClient.Capabilities.cs`（~1330 行）机械拆分为 6 个 partial 文件（TextToImage/Rerank/Video/Tts/WebSocket/Embedding），纯文件移动无逻辑改动（全量测试与拆分前一致）。
> - **测试覆盖补强**：补 6 个公共类型测试共 41 项（`DelegatingChatClientTests` 6 / `BuiltinProviderTests` 3 / `ChatHandlerChainTests` 9 / `CodingModelsTests` 6 / `CopilotSkillLoaderTests` 11 / `CodingAgentTests` 6）；`CodingPlan.FromJson` 对非法 JSON 改为返回空规划（LLM 输出不可控，公共 API 自带容错，与 ToJson 对称）。
> - **性能基准基建**：新建 `NAI/Benchmark` 项目（net10.0 + BenchmarkDotNet 0.15.8，InProcessEmitToolchain），覆盖 11 个关键路径方法；报告见 [性能测试报告](性能测试报告.md)。其余路径（VectorData/MessageParse/SchemaBuild/BuildBody/ToolLoop）均健康（µs 级、KB 级分配、无 Gen2），无需优化。
> - **遗留事项**：`NewLife.AI.csproj` 的 `AssemblyVersion=1.5.*` 通配符与 Deterministic 构建不兼容（CS8357，BenchmarkDotNet 样板工程强制 Deterministic=true 触发），建议评估改为固定 AssemblyVersion（需确认 NewLife 版本体系约定）。
> - 全量测试 **1607 通过 / 0 失败 / 33 跳过**（+43）。
>
> **第 15 轮（2026-08-15）补充**：**新增轻量对话服务层（AiChat 下沉）**。从魔方 AiChat（`NewLife.Cube/AI`，页面内嵌 AI 助手）提取编排逻辑下沉到 NAI core，补齐 `ToolChatClient`（积木）与 `MessageFlow`（完整档）之间的轻量档，供其他项目零复制复用。新增组件：
> - `NewLife.AI/Models/AiChatRequest.cs` — 通用请求 DTO（SessionId/Message/Think/Stream + IExtend Items）
> - `NewLife.AI/Services/ChatSessionService.cs` + `IChatSessionStore.cs` + `CacheSessionStore.cs` — 会话历史（上限 30、可插拔存储；默认实现 `CacheSessionStore` 注入 `ICacheProvider` 自适应单机 MemoryCache / 分布式 Redis）
> - `NewLife.AI/Services/AiChatService.cs` — 编排服务：会话 + ToolChatClient 工具循环 + 流式/非流式统一产出规范 `ChatStreamEvent`；异常经 `ChatErrorHelper.Classify` 映射；空响应兜底；done/error 补工具名
> - `NewLife.AI/Models/ChatStreamEventProjector.cs` — 规范事件 → 轻量前端协议投影（meta/text/thinking/tool/error/done），魔方前端零改动
> - `NewLife.AI/Clients/AiClientRegistryExtensions.cs` — `CreateClientWithFallback`：未注册服务商回退 OpenAI 兼容（提取魔方 `AIService.CreateClient` 逻辑）
> - `NewLife.AI.Extensions/Web/SseHelper.cs` — SSE 输出（头 + 20s 心跳 + 超长截断 + 错误兜底），提取自 ChatAI 与宿主应用逐字重复的 `ChatApiControllerBase.StreamEventsAsync`
> - 迭代器约束处理：`AiChatService` 采用 MessageFlow 同款 moveNext+catch 模式（yield 不可进 try-catch），核心子迭代器（Stream/OnceCoreAsync）无 try-catch 异常上抛；net45/ns2.0 兼容：`TakeLast`/`RemoveRange` 换用 `Skip`/`RemoveAt` 循环
> - 新增测试 34 项：`AiChatServiceTests` 12 / `ChatSessionServiceTests` 6 / `ChatStreamEventProjectorTests` 7 / `AiClientRegistryExtensionsTests` 3 / `SseHelperTests` 6；全量测试 **1648 通过 / 0 失败 / 33 跳过**（+41）。
>
> **第 15 轮续（2026-08-15）**：**生态共用与魔方消费落地**。
> - 提取 `NewLife.AI/Models/ChatStreamEventMapper.cs`（`MapToolEvent` / `MapDelta`），`AiChatService` 与 `MessageFlow.InvokeLlmAsync` 共用（消除工具事件/增量映射重复）；+6 测试（`ChatStreamEventMapperTests`）
> - ChatAI / 宿主应用的 `ChatApiControllerBase` 委托 `SseHelper`（保留 `SetSseHeaders` / `StreamEventsAsync` / `WriteSseEventAsync` 方法签名，调用方零改动），删除各自 ~150 行重复 SSE 实现；两个 csproj 引用 `NewLife.AI.Extensions`
> - `NewLife.AI.Extensions` 目标扩为 **net6.0;net7.0;net8.0;net10.0**（Keyed 服务已 `#if NET8_0_OR_GREATER` 条件编译），`SseHelper.SetSseHeaders` Content-Type 统一为 `text/event-stream; charset=utf-8`
> - 魔方仓库（`NewLife.Cube` / `NewLife.CubeNC`）消费改造：删魔方版 `AiChatRequest`（改 `CubeAiChatRequest : NAI AiChatRequest` 承载页面字段 + `BrowserOpResult`）；`IAIChatService`/`IEntityAiContext`/`IFormAiContext` 统一 NAI 请求；`CubeAIChatService` 委托 `AiChatService` + `ChatStreamEventProjector` 投影（前端协议零改动）+ `CubeChatSessionStore`（MemoryCache 1h）；`AIService.CreateClient` → `CreateClientWithFallback`；`AiController` 复用 `SseHelper.SetSseHeaders`；魔方测试适配（流式响应用 Delta 块）
> - 验证：NAI 全量 **1654 通过 / 0 失败 / 33 跳过**；魔方 `NewLife.Cube` 全目标 / `CubeNC` / 魔方 XUnitTest 均 0 错误，AI 测试 7 通过（全量 8 失败均为外部依赖 QyWeiXin/SsoClient，与改动无关）
> - 遗留：魔方开发期以源码引用 NAI，NAI 发版后切回 NuGet 包
>
> **第 15 轮续二（2026-08-17）**：**SseHelper 撤出 Extensions，下沉 core 纯逻辑**（用户否决三应用依赖尚不稳定的 `NewLife.AI.Extensions`）。
> - 新增 `NewLife.AI/Web/SseEventStream.cs` — SSE 写循环**纯逻辑骨架**（net45/ns2.0/2.1 兼容，无 ASP.NET/序列化依赖）：`StreamAsync` 心跳循环（20s 保活 + 取消/异常统一处理）+ `TruncateField` / `TruncateLargeFields` 截断纯函数；事件写入/心跳/错误推送均由宿主回调注入
> - 删除 `NewLife.AI.Extensions/Web/SseHelper.cs`；`Extensions` 目标回退 **net8.0;net10.0**（撤销为魔方引用而加的 net6/7）
> - ChatAI / 宿主应用的 `ChatApiControllerBase` 恢复各自 ASP.NET 适配层（`SseJsonOptions` / `SetSseHeaders` / `WriteSseEventAsync` 序列化+HttpResponse），`StreamEventsAsync` 委托 core `SseEventStream.StreamAsync`（各应用 MapError 保持原始行为：ChatAI 经 `ChatErrorHelper.Classify`，宿主应用原样输出）
> - 魔方 `AiController` 头设置恢复本地 4 行；三应用（ChatAI/宿主应用/魔方 Cube+CubeNC）与 XUnitTest 均解除 `NewLife.AI.Extensions` 引用
> - **修复用户取消死循环 bug**：`SseEventStream.StreamAsync` 取消后 `Task.Delay` 恒为 canceled，`WhenAny` 永远命中心跳分支 → 死循环；循环开头加 `IsCancellationRequested` 检查即退出（原 SseHelper 靠"写心跳时用取消 token 抛异常"偶然退出，纯逻辑版暴露）
> - 测试：`SseHelperTests` 6 项 → `SseEventStreamTests` 7 项（内存回调模拟：事件写入/心跳/异常映射/用户取消/截断）；验证 NAI 全量 **1655 通过 / 0 失败 / 33 跳过**，魔方 0 错误、AI 测试 7 通过
>
> **第 15 轮续三（2026-08-17）**：**按用户 4 项否决做减法（去冗余抽象）**。
> - **删除 `CreateClientWithFallback`**（用户：code 未注册应报错让管理员修正，fallback 掩盖配置错误）：`AiClientRegistry.CreateClient` 未注册本就抛 `ArgumentException`，魔方 `AIService.CreateClient` 改回直连
> - **删除 `ChatStreamEventMapper`**（转换必要但独立抽象价值低）：`MapToolEvent`/`MapDelta` 内联回 `AiChatService`（私有 `ToToolEvent`），`MessageFlow.InvokeLlmAsync` 恢复原始 switch
> - **删除 `ChatStreamEventProjector` + `LightChatEvent`**（用户选彻底统一规范协议）：魔方 `CubeAIChatService` 直接输出规范 `ChatStreamEvent` JSON（System.Text.Json + camelCase + `SafeInt64Converter`，`message_start` 承载 model）；魔方前端 `ai-assistant.js` 事件解析改为 `content_delta`/`tool_call_start|done|error`（`run_js` 保留，忽略 `message_start`/`message_done`/`thinking_delta`/`heartbeat`）
> - **`AiChatService` 重构**：删 `hasText` 死变量（`hasOutput` 承担兜底判断）；拆 `BuildMessages()`（消息组装/会话写入从 `ChatAsync` 提出）；映射内联
> - 验证：NAI 全量 **1639 通过 / 0 失败 / 33 跳过**（删 3 个测试文件 16 项）；魔方 `NewLife.Cube`/`CubeNC`/XUnitTest 0 错误、AI 测试 7 通过
>
> **第 15 轮续四（2026-08-17）**：**`SseEventStream` 改为实例配置模式**（用户：`StreamAsync` 8 个位置参数可读性差，不如精简前简单易懂）。
> - 重构为同名双类（`Task`/`Task{T}` 模式）：泛型 `SseEventStream<T>`（实例配置类，属性 `Write`/`Heartbeat`/`HeartbeatIntervalMs`/`MapError`/`ErrorCode`/`OnError` + `RunAsync(events, ct)`）与静态 `SseEventStream`（常量 `DefaultHeartbeatIntervalMs`/`MaxFieldLength` + 截断纯函数 `TruncateField`/`TruncateLargeFields`）
> - ChatAI / 宿主应用的 `ChatApiControllerBase.StreamEventsAsync` 改为对象初始化器显式声明回调（`new SseEventStream<ChatStreamEvent> { Write = ..., Heartbeat = ..., MapError = ..., ... }.RunAsync(...)`），意图一目了然、顺序无关
> - `SseEventStreamTests` 7 项改为实例模式（`new SseEventStream<String> { ... }.RunAsync(events, ct)`），截断纯函数静态调用不变
> - 验证：NAI 全量构建 0 错误；星语 `星语.sln` 0 错误；魔方 Cube 0 错误；`SseEventStreamTests` 7/7 通过
>
> **第 15 轮续五（2026-08-17）**：**取消 `SseEventStream`，`ChatApiControllerBase` 自包含 + 宿主应用链接共用**（用户：既然两基类能共用，中间抽象 SseEventStream 就不必要）。
> - 宿主应用 / ChatAI 的 `ChatApiControllerBase` 共用：宿主应用 csproj `<Compile Include="..\NAI\NewLife.ChatAI\Controllers\ChatApiControllerBase.cs" />` + `GlobalUsings.cs` 加 `global using NewLife.ChatAI.Controllers`（一行覆盖全部 40+ 继承控制器），删除宿主应用自己的副本。两版唯一实质差异 MapError 已统一：ChatAI 从 `ChatErrorHelper.Classify` 改为 `ex.Message` 原文（与宿主应用一致，用户确认）
> - **删除 `SseEventStream`**（core `Web/SseEventStream.cs` + `SseEventStreamTests` 7 项）：唯一消费者是两个基类，共用后 SSE 写循环（心跳 20s / 取消 / 异常 / 截断）内联回归 `ChatApiControllerBase.StreamEventsAsync` 自包含实现（保留 `IsCancellationRequested` 防死循环检查），方法签名不变调用方零改动
> - 魔方不拷贝（用户确认）：`AiController.RunSseAsync` 管道结构不同（JSON 字符串写回调 + run_js 带外 + 无心跳 + IAIChatService），拷贝无消费者且是独立 NuGet 库有同步负担
> - 验证：NAI 全量 **1632 通过 / 0 失败 / 33 跳过**（-7）；星语 `星语.sln` 0 错误；NAI 构建 0 错误
>
> **第 15 轮续六（2026-08-17）**：**拆分普通控制器基类与 SSE 控制器基类**（用户：50+ 控制器继承含 SSE 的基类没必要，仅需 SSE 的才继承）。
> - `ChatApiControllerBase` 瘦身为普通基类（`[ApiController]` + 登录校验 + `GetCurrentUserId` / `IsCurrentUserSystem`），移除全部 SSE 内容；新增 `ChatSseControllerBase : ChatApiControllerBase` 承载 SSE 写循环（`SseJsonOptions` / 20s 心跳 / `SetSseHeaders` / `StreamEventsAsync` / `WriteSseEventAsync` / 截断 / 错误兜底）
> - 仅 3 个真用 SSE 的控制器改继承 `ChatSseControllerBase`：ChatAI `MessagesController`、宿主应用 `MessagesController`、`SubAgentProgressController`；其余 50+ 控制器零改动继续继承普通基类（无任何控制器覆盖登录校验，普通基类完全通用）
> - 宿主应用 csproj 增加 `ChatSseControllerBase.cs` 链接（两基类均链接共用）
> - 验证：NAI 全量 **1632 通过 / 0 失败 / 33 跳过**（不变）；星语 `星语.sln` 0 错误

---

## 2. 维度总览

| 编号 | 维度 | 目录 | 审计状态 | 问题数 |
|:----:|------|------|:--------:|:------:|
| D1 | 协议客户端（残留） | `Clients/` | ✅ 已审已修 | 15 |
| D2 | 工具调用引擎 | `Tools/` | ✅ 已审已修 | 2 |
| D3 | IM 渠道 | `Channels/` | ✅ 已审已修 | 9 |
| D4 | 智能体框架 | `Agents/` | ✅ 已审已修 | 8 |
| D5 | 编程智能体 | `Coding/` | ✅ 已审已修 | 7 |
| D6 | 嵌入 | `Embedding/` | ✅ 已审已修 | 2 |
| D7 | 过滤器 | `Filters/` | ✅ 已审（无问题） | 0 |
| D8 | Handler 链 | `Handlers/` | ✅ 已审已修 | 1 |
| D9 | 业务接口契约 | `Interfaces/` | ✅ 已审已修 | 1 |
| D10 | 记忆存储 | `Memory/` | ✅ 已审（无问题） | 0 |
| D11 | MCP | `ModelContextProtocol/` | ✅ 已审已修 | 14 |
| D12 | 模型 DTO / 服务 / 规划器 | `Models/` + `Services/` + `Planner/` | ✅ 已审已修 | 13 |

> 状态标记：✅ 已闭合 | 🟡 部分闭合 | ⏳ 待审 | 🔧 修复中

---

## 3. 问题登记表

> 严重级：**P0** 崩溃/计费错误/安全 | **P1** 功能缺陷/一致性 | **P2** 代码卫生/健壮性/性能
> 状态：🆕 已登记 | 🔧 修复中 | ✅ 已修复 | ⏸ 暂缓

| 编号 | 严重级 | 维度 | 问题 | 修复方案 | 状态 |
|:----:|:------:|:----:|------|----------|:----:|
| A-01 | P1 | D3 Channels | `QQChannel.SendMessageAsync` 每次请求 `using var httpClient = new HttpClient()`（连接泄漏反模式），其余渠道均用 `_clients` 缓存 `ApiHttpClient` | 统一为按 botToken 缓存 `HttpClient` 实例 | ✅ |
| A-02 | P1 | D3 Channels | `DiscordChannel` 用 `static readonly HttpClient _http = new()`，与其余渠道缓存模式不一致 | 统一为按 botToken 缓存 `HttpClient` 实例 | ✅ |
| A-03 | P1 | D1 Clients | `BedrockChatClient.SetHeaders` 用 `ReadAsStringAsync().GetAwaiter().GetResult()` 同步阻塞读取请求体（潜在死锁/线程池饥饿风险） | 分析确认 StringContent 内存读取返回已完成 Task 不阻塞线程；同步签名限制下为安全标准做法 | ⏸ 暂缓 |
| A-04 | P2 | D3 Channels | 11 个渠道仅 1 个测试文件（`ChannelTests`），覆盖薄 | 按渠道补纯逻辑测试（微信/QQ/Discord/Telegram/Slack/WeComBot + 原有 4 渠道，共 12 个新测试） | ✅ |
| A-05 | **P0** | D3 Channels | `WeChatMpChannel.SendMessageAsync` 中 `var secret = "";` 硬编码空字符串，`GetAccessTokenAsync(appId, "")` 必然失败，公众号消息发送功能**完全不可用**；注释声称「由外部服务注入缓存」但 `_tokenCache` 是 private static 无注入机制 | 增加 `AppSecret` 可配置属性，空值时明确报错 | ✅ |
| A-06 | **P0** | D3 Channels | `WeChatKfChannel.SendMessageAsync` 同样 `var secret = "";` 硬编码空字符串，微信客服发送功能**完全不可用** | 同 A-05（增加 `AppSecret` 属性） | ✅ |
| A-07 | P2 | D3 Channels | `QQChannel._tokenCache` 只写不读（`_tokenCache[botAppId] = botToken` 后从未读取），死代码 | 删除死代码（随 A-01 重构） | ✅ |
| A-08 | P2 | D3 Channels | `WebhookChannel.ValidateConfigAsync` 恒返回 `true`，配置校验形同虚设 | 校验为合法 http/https URL；更新测试 | ✅ |
| A-09 | P2 | D3 Channels | `WeChatMpChannel`/`WeChatKfChannel` 用字符串搜索解析 JSON（`IndexOf("\"access_token\":\"")`），脆弱易错 | 改用 `ToJsonEntity` 结构化解析 | ✅ |
| A-10 | P2 | D2 Tools | `ToolSchemaBuilder.BuildTypeSchema` 对复杂类型递归展开无深度限制，极端嵌套类型可能栈溢出 | 增加递归深度上限 `MaxDepth=8` | ✅ |
| A-11 | P2 | D2 Tools | `ToolHelper.IsSsrfRisk` 只检查字面 IP/主机名，不解析 DNS（`example.com` 等域名返回 false，域名指向内网时绕过 SSRF 防护） | 增加 DNS 解析检查 + IPv6 私网段 + 进程级缓存；补测试 | ✅ |
| A-12 | **P1** | D4 Agents/Coding | `CodingPlan.ToJson()` 方法体调用 `this.ToJson()`，C# 成员查找优先于扩展方法，构成**无限递归**，调用即 StackOverflow | 改为显式调用 `JsonHelper.ToJson(this)`；FromJson 补 null 防护（A-24 合并） | ✅ |
| A-13 | **P1** | D4 Agents/Coding | `CodingTools` 工作区沙箱 `StartsWith` 前缀匹配**未校验目录分隔符边界**，`C:\X\MyApp2\...` 等前缀相同兄弟目录被判定为工作区内，沙箱可绕过 | 校验前缀后为目录分隔符或精确相等；补测试 | ✅ |
| A-14 | P2 | D4 Agents/Coding | `Agents/FunctionCallingPlanner` 重规划逻辑：第二次遍历步骤失败直接 `break`，`MaxReplanning`（默认 2）实际最多只发生 1 次重规划，预算浪费 | 二次遍历失败也接入重规划循环 | ✅ |
| A-15 | P2 | D4 Agents/Coding | `ConversableAgent.MaxAutoReply` 只写不读的死属性，注释宣称的"自动工具循环"未实现 | 删除；更新类注释与测试 | ✅ |
| A-16 | P2 | D4 Agents/Coding | `ParallelGroupChat` 所有并行 worker 共享同一 `List<AgentMessage>` 引用，worker 改写历史时并发竞态 | 每个 worker 持有独立副本 | ✅ |
| A-17 | P2 | D4 Agents/Coding | `ParallelGroupChat` 裸 `catch { }` 吞异常无日志，工作代理失败原因完全不可见 | XTrace 记录异常与超时 | ✅ |
| A-18 | P2 | D4 Agents/Coding | `CodingTools` 池化 `StringBuilder` 在超时/异常路径未归还（`RunCommandAsync`/`GetErrorsAsync`），对象池实例流失 | 所有路径归还 | ✅ |
| A-19 | P2 | D4 Agents/Coding | `CodingTools.RunCommandAsync` 硬编码 `cmd.exe`，Unix/macOS 上 `run_command` 失效 | 按 `Environment.OSVersion` 分支 | ✅ |
| A-20 | P2 | D4 Agents/Coding | `CodingTools.GetErrorsAsync` 魔法数字 `120_000` 与 `RunCommandAsync` 的 `600_000` 超时上限不一致 | 抽常量 `MaxCommandTimeoutMs` 统一 | ✅ |
| A-21 | P2 | D4 Agents/Coding | `CodingTools` 搜索遇无权限目录 `UnauthorizedAccessException` 被捕获后**整个搜索返回 Error** 而非跳过继续 | 新增 `EnumerateFilesSafe` 递归跳过无权限目录 | ✅ |
| A-22 | P2 | D4 Agents/Coding | `CodingTools.ReadFileAsync` 行范围截取仍 `File.ReadAllLines` 全量读文件，大文件浪费 | 改用 `File.ReadLines` 惰性按需读取 | ✅ |
| A-23 | P2 | D4 Agents/Coding | `CodingAgent._currentPhase` 实例级共享可变状态，工具回调线程并发读写竞态；异常退出时残留上一阶段名 | **误报关闭**：`String` 引用读写原子无撕裂，阶段名仅展示用途；实例级锁也无法解决并发语义，已回滚 | ✅ |
| A-24 | P2 | D4 Agents/Coding | `CodingPlan.FromJson` 无 null 入参防护，json 为 null 时 NRE | 加 null 校验（随 A-12 修复） | ✅ |
| A-25 | P2 | D4 Agents/Coding | `CopilotSkillLoader.MatchInstructions` 无调用方，且 `MatchGlob("Doc/**")` 相对 glob 匹配绝对路径必然失败 | 修正 `MatchGlob` 支持绝对路径 + `**/` 前缀 | ✅ |
| A-26 | P2 | D4 Agents/Coding | `Agents/FunctionCallingPlanner.cs:287` 死代码：`var raw = json.TrimStart('[').TrimEnd(']');` 从未使用 | 删除 | ✅ |
| A-27 | **P1** | D11 MCP | `McpServer._handler` 与 `Manager` 完全断连（`new McpHandler()` 孤儿实例 Host 恒 null），`IApiManager` 从未注册，**每一次 `tools/call` 都返回 NotFound**，工具调用必然失败 | 继承 `ApiHost` 使 `_handler.Host=this`，`GetService(IApiManager)` 返回 `Manager`；配套修复 A-29/30/37 | ✅ |
| A-28 | P2 | D11 MCP | `McpServer` catch 分支从不写日志（类实现 ILogFeature 却不用），内部异常 `ex.Message` 原样回写客户端泄露实现细节 | 写日志（A-27 继承 ApiHost 后自动获得） | ✅ |
| A-29 | P2 | D11 MCP | `McpServer` 对 JSON-RPC notification（`notifications/initialized` 无 Id）回响应，违反 JSON-RPC 2.0 规范；测试还把该错误行为固化为断言 | 区分 notification 不响应；更新测试 | ✅ |
| A-30 | P2 | D11 MCP | `McpServer` 生成的 `SessionId` 被丢弃（`Rand.NextString(16)` 后从未回写），会话握手机制名存实亡 | 将新 SessionId 写入响应头 | ✅ |
| A-31 | P2 | D11 MCP | `McpServer.Process` 全链路同步阻塞异步（`_handler.Execute` 内部 `GetAwaiter().GetResult()`），工具为 async Task 时线程池饥饿风险 | 保持同步 API（公共签名）；内存 body 读取不阻塞线程 | ⏸ 暂缓 |
| A-32 | P2 | D8 Handlers | `ChatHandlerChain` 懒缓存 `??=` 写入无锁，读线程构建期间另一线程 `Invalidate` 置 null，旧列表写回后**缓存永久丢失新处理器** | `GetHandlers` 锁内双检 | ✅ |
| A-33 | P2 | D11 MCP | `McpServer` IProgress 支持名存实亡：工具内解引用 progress 即 NRE，`ProgressNotification`/`ProgressParams` 为死代码 | 删除 `ProgressNotification`/`ProgressParams` 死代码；IProgress 为框架限制，工具不应依赖进度 | ✅ |
| A-34 | P2 | D11 MCP | `McpServer` 协议校验缺失：未校验 `JsonRpc == "2.0"`、未区分 notification、`tools/call` 无鉴权与会话绑定 | 补 JsonRpc 版本校验；notification 不响应（A-29） | ✅ |
| A-35 | P2 | D11 MCP | `HttpMcpServer` 的 `context.Connection!` 可空强制解引用 NRE 风险 | 判空 + XTrace 记录 | ✅ |
| A-36 | P2 | D11 MCP | `HttpMcpServer`/`AspNetMcpServer` 请求体无大小限制，超大 body 可打爆内存（DoS） | `HttpMcpServer` 增加 1MB body 上限 | ✅ |
| A-37 | P2 | D11 MCP | `McpToolManager.ToSnakeCase` 对连续大写缩写处理错误（`GetAPIKey` → `get_apikey`），`APIKey`/`Apikey` 碰撞成同一工具名 | 修正缩写处理 | ✅ |
| A-38 | P3 | D11 MCP | `ToolInfo` 空类死代码，全库无引用 | 删除 | ✅ |
| A-39 | P3 | D11 MCP | `ProtocolModel` 的 `InitializeParams`/`ProgressNotification`/`ProgressParams` 三个 record 死代码 | 删除 `ProgressNotification`/`ProgressParams`（`InitializeParams` 被 ChatAI 引用保留） | ✅ |
| A-40 | P3 | D11 MCP | `McpServer.AddTool<TTools>` 参数从未使用；`MapMcp<TTools>` 端到端从未调用 `AddTool`，工具列表恒为空 | `MapMcp` 创建 server 后调用 `AddTool<TTools>` | ✅ |
| A-41 | P3 | D11 MCP | `McpContext.HostContext` 只写不读 | 保留（供 HTTP 宿主传上下文，属扩展点） | ⏸ 暂缓 |
| A-42 | **P1** | D12 Planner | `Planner/FunctionCallingPlanner.ExecuteAsync` 内层 `catch (Exception)` 吞掉 `OperationCanceledException`，取消被记录为 Failed 而非取消；外层 `catch (OperationCanceledException)` 死代码 | 内层优先 `catch (OperationCanceledException) { throw; }`；补测试 | ✅ |
| A-43 | **P1** | D12 Embedding | `VectorData.ToVector()` 对脏数据无保护：非法 base64 抛 `FormatException`，长度非 4 倍数时 `BlockCopy` 抛 `ArgumentException`，RAG 读取脏数据即崩溃 | `BlockCopy` 拷贝长度截断 + 非法 base64 返回空数组；补测试 | ✅ |
| A-44 | P2 | D12 Planner | `Planner/FunctionCallingPlanner._lock` 声明但从未使用（死代码），`ExecuteAsync` 并发修改 `Status` 无同步 | 删除 `_lock` | ✅ |
| A-45 | P2 | D12 Planner | `IPlan.SetFinalAnswer` 无任何调用者，`IPlan.FinalAnswer` 永远为 null，接口承诺未实现 | `ExecuteAsync` 完成后填充 `FinalAnswer`（最后成功步骤结果） | ✅ |
| A-46 | P2 | D12 Services | `BackgroundGenerationService` 延迟清理仅按 messageId 匹配，同 Id 新任务窗口内注册会被旧清理回调误清空 | 校验 `removed == task`（ReferenceEquals） | ✅ |
| A-47 | P2 | D12 Services | `BackgroundGenerationService` 重复注册同 messageId 时旧 CTS 泄漏（不 Cancel/Dispose） | 注册前 TryRemove 并取消旧条目 | ✅ |
| A-48 | P2 | D12 Embedding | `HashTextEmbedder` 类注释写 `local-hash-v1`，实现为 `local-hash-v2`，`VectorData` 注释也全写 v1，命名不一致导致 v1→v2 迁移语义误判 | 统一注释为 v2 | ✅ |
| A-49 | P2 | D9 Interfaces | `IAppKey.ExpireTime`/`IUserMemory.ExpireTime` 类型为 `DateTime` 但注释写"null表示永不过期"，契约矛盾 | 修正注释为 `DateTime.MinValue` 表示永不过期（XCode 实体值类型约束） | ✅ |
| A-50 | P2 | D6 Embedding | `EmbeddingClientMetadata.ProviderName = null!` init-only 属性无保护，外部构造后不赋值即使用 NRE | 改为构造器注入强约束 | ✅ |
| A-51 | P2 | D12 Services | `MessageRateLimiter` 注释称"后台定期清理"，实现仅在 `count % 200 == 1` 惰性清理，请求停止后过期桶永久残留 | 修正注释与实现一致 | ✅ |
| A-52 | **P1** | D12 Models | `ChatResponse.Text` 空内容列表（`[]`，tool_calls/content_filter 结束场景）时 `list.FirstOrDefault()` 返回 null，落到 `value.ToString()` 抛 NRE | `FirstOrDefault()` 后再判空；补测试 | ✅ |
| A-53 | **P1** | D12 Models | ~~`ChatRequest.Create` 与 `ToolChatClient.MergeToolOptions` 对 `Items` 字典按引用别名共享，污染调用方原始 request~~ | **误报关闭**：Items 共享是**故意设计**（协议层写入 request["xxx"] 同步回调用方，多轮持续保留协议键值）；已恢复直接引用并加防回归注释 | ✅ |
| A-54 | P2 | D12 Models | `ChatRequest.Create` else 分支为不可达死代码（新建 request.Items 恒为空字典，恒真短路） | **随 A-53 误报关闭**：改为直接引用共享，无 else 分支 | ✅ |
| A-55 | P2 | D12 Models | `ChatOptions`/`EmbeddingModelSetting` 索引器未防御 `Items` 为 null，与 `ChatMessage` 索引器防御不一致 | 统一 `get` 判空 / `set` `??=` 创建 | ✅ |
| A-56 | P2 | D12 Models | `ChatMessage.ParseMultimodalContent` catch 吞异常无日志无埋点，解析失败排障无据 | 补 XTrace 日志 | ✅ |
| A-57 | P2 | D12 Models | `ChatResponse.AddToolCallDelta` 硬编码 `Index = 0`，与 `Add`/`AddDelta` 用 `msgs.Count` 不一致，多工具流式块产生多个 Index=0 的 choice | 用 `msgs.Count` | ✅ |
| A-58 | **P1** | D1 Clients | `DashScopeRealtimeClient.RealtimeEvent.Parse` 用抛异常字典索引访问可选键（`dic["response"]`/`dic["item_index"]`），首个 `session.created` 事件即抛 KeyNotFoundException，**接收循环必然中断** | 全部改用 `TryGetValue` | ✅ |
| A-59 | P2 | D1 Clients | `AiClientBase.Dispose()` 空实现，HttpClient（含 handler/连接池）从不释放，频繁 CreateClient 时连接堆积 | Dispose 释放内部创建的 HttpClient（`_ownsHttpClient` 区分外部注入） | ✅ |
| A-60 | P2 | D1 Clients | `AiClientRegistry` 两个 Dictionary 无锁并发，`Default` 公共可变单例运行时并发 Register+查询抛异常 | 改用 `ConcurrentDictionary` | ✅ |
| A-61 | P2 | D1 Clients | `DashScopeRealtimeClient._cts` 死状态：Token 从未被订阅，Dispose 的 `_cts?.Cancel()` 空操作，接收循环只能靠非预期异常中断 | 接收循环链接 `_cts.Token` | ✅ |
| A-62 | P2 | D1 Clients | `DashScopeRealtimeClient.ConnectAsync` 连接失败泄漏 `ws` | try/catch 中 `ws.Dispose()` | ✅ |
| A-63 | P2 | D1 Clients | `ChatClientBuilder.SetInnerClient` 静默覆盖已有内层客户端且不释放，配置被吞无告警 | 覆盖前释放旧客户端（非同一实例） | ✅ |
| A-64 | P2 | D1 Clients | `ChatCompletionRequest.ToChatRequest` 与 `FromChatRequest` 不对称：丢 `Name`/`ToolCallId`/`ReasoningContent`，tool 消息回送 400 / DeepSeek 思考回传违反约束 | 补三字段复制 | ✅ |
| A-65 | P3 | D1 Clients | `AiClientRegistry` 同 Code 重注册且 DisplayName 变化时 `_displayNames` 旧条目残留 | 重注册时清理旧 DisplayName | ✅ |
| A-66 | P3 | D1 Clients | `AiClientRegistry.Register(Assembly)` 的 `assembly.GetTypes()` 遇 `ReflectionTypeLoadException` 未处理 | try-catch 降级用异常子集 | ✅ |
| A-67 | P3 | D1 Clients | `AiClientBase.GetResponseAsync`/`GetStreamingResponseAsync` 直接改写调用方 `request.Stream`/`request.Model`，同一 request 先流式后非流式互相翻转 | 临时改写 + finally 恢复 | ✅ |
| A-68 | P3 | D1 Clients | `AiClientBase.CombineApiUrl` public API 对无前导 `/` 的 path 拼出畸形 URL | 规范化 path（无前导斜杠时补 `/`） | ✅ |
| A-69 | P3 | D1 Clients | `FilteredChatClient` 火焰即忘 `Task.Run` 内 `catch { }` 空吞 `OnStreamCompletedAsync` 异常，后处理失败不可见 | XTrace 记录异常 | ✅ |
| A-70 | P3 | D1 Clients | `ChatCompletionRequest.BuildBody`/`FromChatRequest` 只读 `request.MaxTokens`，忽略 `MaxCompletionTokens`，该属性经 DashScope/DeepSeek 路径被静默丢弃 | 确认 R4-2 已覆盖三路径（FromChatRequest/BuildBody/ToChatRequest），误报 | ✅ |
| A-71 | P3 | D1 Clients | `DashScopeRealtimeClient` 单条 WS 消息含多个 JSON 事件（服务端批量推送拼接）时，整条 `JsonParser.Decode` 解析失败被静默丢弃 | 新增 `SplitJsonObjects` 按顶层大括号配对切分（忽略字符串值内大括号与转义），逐片段 `RealtimeEvent.Parse`；尾部残缺片段保持丢弃语义，不引入复杂错误路径 | ✅ |
| A-72 | P3 | D1 Clients | `AiClientBase._host` 非 readonly 可变静态状态 | 改 readonly | ✅ |
| A-73 | **P1** | D2 Tools | `ToolChatClient` 实例级状态跨请求残留（`IsTotalTokenLimitExceeded`/`IsToolLoopLimitExceeded`/`_fallbackEstimatedTokens`/`_consecutiveFailureRounds` 只置位不重置），上一请求超限永久禁用后续兜底产出、Token 估算跨请求累加 | 两入口统一重置 | ✅ |
| A-74 | **P1** | D2 Tools | `WebFetchDirectService` HttpClient 自动重定向绕过 SSRF 校验；`IsSsrfRisk` DNS 解析失败放行 + 永久缓存 null 放大 DNS rebinding | 禁用重定向 + 解析失败视为风险 + 仅缓存非空结果 | ✅ |
| A-75 | **P1** | D2 Tools | `ToolRegistry.GetTools` 过滤语义与契约相反：空集合返回全量而非仅系统工具；非空集合不含系统工具 | 按契约重写（空→仅系统；非空→系统∪指定） | ✅ |
| A-76 | **P1** | D2 Tools | `ToolChatClient` 非流式路径不应用 `ToolResultMaxChars` 截断（流式有、非流式无），大工具结果撑满上下文 | 非流式包 `TruncateResult` | ✅ |
| A-77 | P2 | D2 Tools | 流式 Token 超限 `yield break` 分支泄漏池化 `StringBuilder` | 超限前归还 | ✅ |
| A-78 | P2 | D2 Tools | `MergeToolCallDelta` 丢弃无 Id 的首个增量块，该工具调用后续增量全丢 | 无 Id 也创建条目 | ✅ |
| A-79 | P2 | D2 Tools | `InvokeMethodAsync` 不支持 `ValueTask<T>` 返回值，序列化成 `{}` 垃圾 | 加 ValueTask/ValueTask&lt;T&gt; 分支 | ✅ |
| A-80 | P2 | D2 Tools | `options.Tools` 注入 schema 但无路由，模型调用必然失败 | 按名路由到 Provider + 无路由记录警告 | ✅ |
| A-81 | P2 | D5 Coding | `CodingTools` 正则无超时编译，LLM 提供的 pattern 可 ReDoS 挂起代理 | 加 2 秒匹配超时 | ✅ |
| A-82 | P2 | D2 Tools | `TryInvokeAsync` 未注册错误 JSON 未转义工具名，产出非法 JSON | 整体 `ToJson` 转义 | ✅ |
| A-83 | P2 | D2 Tools | `WeatherNmcService` 首次全量扫描无锁，并发首个请求各自扫描 N 份 | 静态信号量 + 双检 | ✅ |
| A-84 | P2 | D1 Clients | ~~`AiClientRegistry` 工厂原地修改调用方 `AiClientOptions.Endpoint`，复用同一 options 创建多服务商端点串台~~ | **误报关闭**：直接引用 opts 是**故意设计**（内部回填默认端点同步反映到调用方，各服务商默认端点外部可见可查）；已恢复直接引用 + 防回归注释 | ✅ |
| A-85 | P2 | D12 Services | `BackgroundGenerationService` 重注册后旧任务 finally 误删/Dispose 新任务 CTS，新任务永久失去取消能力 | finally 按 CTS 实例引用校验 | ✅ |
| A-86 | P2 | D1 Clients | `OllamaChatClient.PullModelAsync` 30 分钟超时被 `HttpClient.Timeout`（默认 300s）先触发，拉大模型必然中断 | 临时放大超时 + finally 恢复 | ✅ |
| A-87 | P2 | D1 Clients | `AiClientBase` 5 个非流式 HTTP 辅助方法不释放 `HttpResponseMessage`，高频调用连接堆积 | `using` 释放 | ✅ |
| A-88 | P2 | D1 Clients | OpenAI 图像/视频、Anthropic、Gemini 硬编码 `/v1` 路径，endpoint 含版本段时 `/v1/v1` 双路径 404 | 统一 `CombineApiUrl`（正则扩展支持 v1beta） | ✅ |
| A-89 | P2 | D1 Clients | `deepseek-reasoner` 能力虚标：声明 `SupportFunction: true` 但 `BuildRequest` 剥离 tools | 能力位如实标注 | ✅ |
| A-90 | P2 | D1 Clients | Ollama 流式终结帧 `FinishReason` 覆写死代码（`Messages` 恒 null 不可达） | 删除死代码 | ✅ |
| A-91 | P3 | D12 Models | `DashScopeRequest.NormalizeTextContent` null 分支两分支相同死代码 | 简化 | ✅ |
| A-92 | P3 | D1 Clients | `DashScopeChatClient.DefaultEndpoint` setter no-op（getter 不读设置值） | 私有覆盖字段生效 | ✅ |
| A-93 | P3 | D1 Clients | `GeminiResponse` 两条转换路径角色不一致（`assistant` vs `model`） | 统一 `assistant` | ✅ |
| A-94 | P2 | D1 Clients | 协议专用请求（Anthropic/Gemini/DashScope/Bedrock/Ollama）`IChatRequest.Messages` 与序列化列表不一致，过滤器链修改被静默丢弃 | **网关统一化方案**：网关入口 `NormalizeGatewayRequest` 将协议请求经 `ToChatRequest()` 转统一 ChatRequest，过滤器链工作于统一对象（天然无写回问题）；曾实现写回代理 `MessageWriteBackList`（5 协议 SyncFromMessages）后评估复杂度不划算已回退 | ✅ |
| A-95 | **P1** | D1 Clients | `DashScopeChatClient.AutoDetectSearchIntent` 把联网意图检测结果写回 `request.Items`（与调用方 options.Items 按设计共享，A-53），跨请求永久污染 → 后续所有请求静默强制联网搜索（行为 + 费用影响） | 检测改为返回值传递（`DetectSearchIntent` → `DashScopeSearchIntent`），不落 Items；兼容路径 `AppendDashScopeFields` 接收 intent 回退，原生路径 `ApplyDetectedIntent` 后处理注入 | ✅ |
| A-96 | P2 | D12 Services | `MessageRateLimiter` `count % 200 == 1` 在 count=1 恒触发 → 每用户每分钟首条消息即全表扫描清理 | 改 `count >= 200 && count % 200 == 0` | ✅ |
| A-97 | P2 | D1 Clients | `AiClientBase.HttpClient` 惰性初始化无锁，并发首访双创建泄漏一个连接池 | 双检锁 + getter/setter/Dispose 统一加锁 | ✅ |
| A-98 | P2 | D1 Clients | 6 处 `resp.Model = request.Model` 无条件覆盖服务端真实 model（OpenAIClientBase/GeminiChatClient×2/NewLifeAiChatClient×2/DashScopeChatClient×2），网关/路由场景计费归属失真 | 全部改 `??=`（仅空时回填） | ✅ |
| A-99 | P2 | D1 Clients | `DashScopeChatClient.Capabilities` TTS 音频下载每次 `new HttpClient`（A-01/A-02 同类反模式，未池化） | 接入 `HttpClientPool` 按主机复用 handler | ✅ |
| A-100 | P3 | D1 Clients | Anthropic/Bedrock `ToChatRequest` 丢 ToolCalls/ToolCallId/ReasoningContent/signature（与 OpenAI A-64 不对称） | Anthropic 从内容块恢复 tool_use/thinking/tool_result/redacted_thinking；Bedrock 从类型化块恢复 toolUse/toolResult；Gemini 补充对称性限制说明 | ✅ |
| A-101 | P3 | D6 Embedding | `VectorData`/`ILocalTextEmbedder` 注释仍写 local-hash-v1，实现已 v2（A-48 只改了 HashTextEmbedder） | 注释统一 v2 | ✅ |
| A-102 | P3 | D1 Clients | 6 处 `public static JsonOptions DefaultJsonOptions` 可变静态字段，外部可全局替换导致跨线程序列化不一致 | 改 `public static readonly`（全仓无赋值点，安全） | ✅ |
| A-103 | P3 | D12 Models | `ChatErrorHelper.IsContextLengthError` 对 net45/netstandard2.0 编译 CS8602（08-05 新增文件，`IsNullOrEmpty` 无 `[NotNullWhen]` 注解） | `message!` 显式声明守卫后的非空 | ✅ |
| A-104 | P3 | D2 Tools | `ToolRegistry.InvokeMethodAsync` 对同一 method 调用两次 `GetParameters()`（反射开销，每次工具调用重复） | 一次获取 `allParams` 后过滤复用 | ✅ |
| A-105 | P3 | D2 Tools | `ToolRegistry.RegisterMethod` 同名工具重复注册时静默跳过（保护首次注册），名称冲突无任何提示，排障困难 | 跳过时 XTrace 警告日志（含来源类型与方法名）；类 remarks 文档化「注册应在启动阶段完成」线程契约 | ✅ |
| A-106 | P1 | D1 Clients | `AnthropicRequest`/`GeminiRequest` 的 `IChatRequest.Tools` 为自动属性（`[IgnoreDataMember]`），入站时原生 tools 数组不转换为 ChatTool 列表——网关统一化 `ToChatRequest()` 后工具定义静默丢失（A-94 前即存在，`CreateGatewayFlowContext` 读取 `request?.Tools` 同路径）；Ollama 有 `_chatTools` 惰性转换不受影响 | 与 Ollama 一致改为惰性转换：原生 Tools 数组（兼容 JsonElement/Dictionary）→ ChatTool 列表，**复用 `CollectionHelper.ToDictionary()`**（JsonElement 递归转字典、大小写不敏感）；Anthropic 取 name/description/input_schema；Gemini 遍历 functionDeclarations；Ollama 遍历 function | ✅ |
| A-107 | P1 | D1 Clients | `GeminiRequest.ApplyResponseFormat` / `OllamaChatRequest.MapResponseFormat` 对 `ResponseFormat` 用 `responseFormat.ToJson()` 再 `JsonParser.Decode`——网关入站 ResponseFormat 为 JsonElement 时 `ToJson()` 序列化为 `{"ValueKind":1}`，`type` 读取失败，**结构化响应格式静默丢失**（json_object/json_schema 不生效） | 复用 `CollectionHelper.ToDictionary()`（兼容 Dictionary/JsonElement 递归转换）；String 单独 `JsonParser.Decode`；嵌套 json_schema 兼容两种字典表示（IDictionary<String,Object?> 与 IDictionary<String,Object>） | ✅ |
| A-108 | P1 | D1 Clients | `DashScopeChatClient.ReceiveWebSocketJsonAsync`（TTS 实时路径：Qwen-TTS-Realtime / CosyVoice）用 `JsonParser.Decode(json)` 解析整条消息——对多顶层对象 `Decode` **静默返回第一个**，后续事件丢失（如 response.audio.delta + response.done 拼接时 done 丢失，可能导致 session.finish 不发送、接收循环卡住）；与 A-71 的 Omni 路径同类 | 复用 `SplitJsonObjects` 切分多事件（新增 `ParseJsonEvents` 静态方法）；单事件直接返回，多事件入按 ws 隔离的 `_pendingEvents` 队列逐次消费；连接结束 finally 清理队列条目防泄漏 | ✅ |

---

## 4. 扫描记录

（按维度记录扫描过程与发现）

### D3 Channels — 扫描记录

- 11 个渠道（DingTalk/WeCom/Feishu/Webhook/Slack/Telegram/Discord/QQ/WeChatMp/WeChatKf/WeComBot）均实现 `IMessageChannel`
- 大部分渠道用 `_clients` ConcurrentDictionary 缓存 `ApiHttpClient`，但 **QQChannel 每次新建 HttpClient**、**DiscordChannel 用静态 HttpClient**
- 测试仅 `XUnitTest/Channels/ChannelTests.cs` 覆盖 WeCom/DingTalk/Feishu/Webhook，其余 7 个渠道无测试
- `SlackChannel.ConvertToSlackMarkdown` 用正则转换 Markdown，有纯逻辑可测
- `TelegramChannel` 的 `GetClient` 按 botToken 缓存

---

## 5. 修复记录

### 第 1 轮（2026-08-04）— P0/P1 修复

| 编号 | 修复内容 | 涉及文件 |
|:----:|----------|----------|
| A-05/A-06 | 微信渠道 `secret` 硬编码空串 → 增加 `AppSecret` 可配置属性，空值时明确报错 | `Channels/WeChatMpChannel.cs`、`WeChatKfChannel.cs` |
| A-12/A-24 | `CodingPlan.ToJson()` 无限递归 → 显式 `JsonHelper.ToJson(this)`；`FromJson` 加 null 防护 | `Coding/Models/CodingPlan.cs` |
| A-13 | 沙箱 `StartsWith` 前缀碰撞绕过 → 校验目录分隔符边界 + 补测试 | `Coding/CodingTools.cs` + `CodingToolsTests.cs` |
| A-27/A-29/A-30/A-34 | `McpServer` 继承 `ApiHost` 绑定 `_handler.Host`，`GetService(IApiManager)` 返回 Manager，`tools/call` 不再 NotFound；notification 不响应；新 SessionId 回写响应头；校验 JsonRpc 2.0 | `ModelContextProtocol/McpServer.cs` |
| A-42 | Planner 取消令牌被吞 → 内层优先 `catch (OperationCanceledException) { throw; }` + 补测试 | `Planner/FunctionCallingPlanner.cs` + `PlannerTests.cs` |
| A-43 | `VectorData.ToVector()` 脏数据崩溃 → BlockCopy 截断 + 非法 base64 返回空数组 + 补测试 | `Embedding/VectorData.cs` + `VectorDataTests.cs` |
| A-52 | `ChatResponse.Text` 空内容数组 NRE → `FirstOrDefault()` 后再判空 + 补测试 | `Models/ChatResponse.cs` + `ChatCompletionResponseTests.cs` |
| A-53/A-54 | ~~`ChatRequest.Create`/`ToolChatClient.MergeToolOptions` Items 别名共享污染 → 逐项复制~~ → **误报关闭**：Items 共享为故意设计，已恢复直接引用 + 防回归注释 | `Models/ChatRequest.cs`、`Tools/ToolChatClient.cs` |
| A-58 | `RealtimeEvent.Parse` 字典索引崩溃 → 全部 `TryGetValue` | `Clients/DashScope/DashScopeRealtimeClient.cs` |

### 第 2 轮（2026-08-04）— 渠道与工具修复

| 编号 | 修复内容 | 涉及文件 |
|:----:|----------|----------|
| A-01/A-07 | `QQChannel` 每次 `new HttpClient()` → 按 botToken 缓存；删除死代码 `_tokenCache` | `Channels/QQChannel.cs` |
| A-02 | `DiscordChannel` 静态 HttpClient → 按 botToken 实例缓存 | `Channels/DiscordChannel.cs` |
| A-08 | `WebhookChannel.ValidateConfigAsync` 恒 true → 校验合法 http/https URL + 更新测试 | `Channels/WebhookChannel.cs` + `ChannelTests.cs` |
| A-09 | 微信渠道字符串搜索解析 JSON → `ToJsonEntity` 结构化解析 | `WeChatMpChannel.cs`、`WeChatKfChannel.cs` |
| A-37 | `McpToolManager.ToSnakeCase` 缩写处理错误 → 修正缩写边界（`GetAPIKey`→`get_api_key`） | `ModelContextProtocol/McpToolManager.cs` |
| A-10 | `ToolSchemaBuilder` 递归无深度限制 → `MaxDepth=8` | `Tools/ToolSchemaBuilder.cs` |
| A-56 | `ChatMessage.ParseMultimodalContent` 吞异常 → XTrace 记录 | `Models/ChatMessage.cs` |
| A-69 | `FilteredChatClient` 空吞后处理异常 → XTrace 记录 | `Clients/FilteredChatClient.cs` |
| A-57 | `AddToolCallDelta` Index 硬编码 0 → `msgs.Count` | `Models/ChatResponse.cs` |
| A-64 | `ChatCompletionRequest.ToChatRequest` 丢 Name/ToolCallId/ReasoningContent → 补齐 | `Clients/OpenAI/ChatCompletionRequest.cs` |
| A-46/A-47 | `BackgroundGenerationService` 清理竞态 + CTS 泄漏 → ReferenceEquals 校验 + 注册前取消旧 CTS | `Services/BackgroundGenerationService.cs` |
| A-48 | `HashTextEmbedder` 注释 v1→v2 | `Embedding/HashTextEmbedder.cs` |
| A-49 | `IAppKey`/`IUserMemory` ExpireTime 注释契约矛盾 → 修正为 MinValue 语义 | `Interfaces/IAppKey.cs`、`IUserMemory.cs` |
| A-50 | `EmbeddingClientMetadata.ProviderName` init+null! → 构造器注入强约束 | `Embedding/IEmbeddingClient.cs` + `OpenAIChatClient.Capabilities.cs` |
| A-51 | `MessageRateLimiter` 注释与实现不符 → 修正注释 | `Services/MessageRateLimiter.cs` |
| A-16/A-17 | `ParallelGroupChat` 共享历史竞态 + 裸 catch → 独立副本 + XTrace 记录 | `Agents/ParallelGroupChat.cs` |
| A-15 | `ConversableAgent.MaxAutoReply` 死属性 → 删除 + 更新测试 | `Agents/ConversableAgent.cs` + `AgentSystemTests.cs` |
| A-14/A-26 | `FunctionCallingPlanner` 重规划预算浪费 + 死代码 → 二次遍历接入重规划循环 + 删死代码 | `Agents/FunctionCallingPlanner.cs` |
| A-18~A-22 | `CodingTools` 池化 StringBuilder 未归还 / cmd.exe 跨平台 / 超时魔法数字 / 搜索无权限目录 / 全量读文件 → 全部修复 | `Coding/CodingTools.cs` |
| A-23 | ~~`CodingAgent._currentPhase` 竞态 → `_phaseLock`~~ → **误报**：`String` 引用原子性无需锁，已回滚 `_phaseLock` | `Coding/CodingAgent.cs` |
| A-25 | `CopilotSkillLoader.MatchGlob` 相对 glob 匹配绝对路径失败 → 支持 `**/` 前缀 | `Coding/CopilotSkillLoader.cs` |

### 第 3 轮（2026-08-04）— Clients/MCP 修复

| 编号 | 修复内容 | 涉及文件 |
|:----:|----------|----------|
| A-59 | `AiClientBase.Dispose` 空实现 → `_ownsHttpClient` 区分释放内部创建的 HttpClient | `Clients/AiClientBase.cs` + `OllamaIntegrationTests.cs`（修复 using 作用域） |
| A-60/A-65/A-66 | `AiClientRegistry` 无锁并发 → `ConcurrentDictionary` + 旧 DisplayName 清理 + GetTypes 异常降级 | `Clients/AiClientRegistry.cs` |
| A-61/A-62 | `DashScopeRealtimeClient` _cts 死状态 + 连接失败泄漏 → 链接 token + try/catch 释放 | `Clients/DashScope/DashScopeRealtimeClient.cs` |
| A-63 | `ChatClientBuilder.SetInnerClient` 静默覆盖 → 释放旧客户端 | `Clients/ChatClientBuilder.cs` |
| A-67 | `AiClientBase` 改写调用方 request.Stream/Model → finally 恢复 | `Clients/AiClientBase.cs` |
| A-72 | `AiClientBase._host` 非 readonly → readonly | `Clients/AiClientBase.cs` |
| A-38/A-39 | MCP 死代码清理：删除 `ToolInfo` 空类、`ProgressNotification`/`ProgressParams` | `ModelContextProtocol/` |
| A-40 | `MapMcp<TTools>` 未注册工具 → 创建 server 后调用 `AddTool<TTools>` | `NewLife.AI.Extensions/McpExtensions.cs` |
| A-33 | MCP IProgress 支持名存实亡 → 评估为框架限制（工具不依赖进度），降级记录 | — |

### 第 4 轮（2026-08-04）— 收尾修复

| 编号 | 修复内容 | 涉及文件 |
|:----:|----------|----------|
| A-32 | `ChatHandlerChain` 懒缓存 `??=` 无锁 → `GetHandlers` 锁内双检 | `Handlers/ChatHandlerChain.cs` |
| A-35 | `HttpMcpServer` Connection! 判空 + A-36 请求体大小限制（1MB DoS 防护） | `ModelContextProtocol/HttpMcpServer.cs` |
| A-11 | `ToolHelper.IsSsrfRisk` 增加 DNS 解析（域名指向内网拦截）+ IPv6 私网段 + 进程级缓存 | `Tools/ToolHelper.cs` + `NativeToolTests.cs` |
| A-45 | `FunctionCallingPlan` 完成后填充 `FinalAnswer`，兑现接口承诺 | `Planner/FunctionCallingPlanner.cs` |
| A-68 | `CombineApiUrl` 无前导斜杠 path 规范化 | `Clients/AiClientBase.cs` |
| A-70 | 确认 MaxCompletionTokens 三路径已覆盖（R4-2），误报关闭 | — |

### 第 6 轮（2026-08-05）— 深扫修复（A-73~A-94）

| 编号 | 修复内容 | 涉及文件 |
|:----:|----------|----------|
| A-73 | `ToolChatClient` 请求级状态跨请求残留 → 两入口统一重置 | `Tools/ToolChatClient.cs` + `NativeToolTests.cs` |
| A-74 | SSRF：WebFetch 自动重定向绕过 + DNS 解析失败放行 + 缓存 null → 禁用重定向 + 保守拒绝 + 仅缓存非空 | `Tools/ToolHelper.cs`、`Tools/IWebFetchService.cs` |
| A-75 | `ToolRegistry.GetTools` 过滤契约相反 → 按契约重写（空→仅系统；非空→系统∪指定） | `Tools/ToolRegistry.cs` + `NativeToolTests.cs` |
| A-76 | 非流式不应用 `ToolResultMaxChars` → 补 `TruncateResult` | `Tools/ToolChatClient.cs` |
| A-77 | 流式超限泄漏池化 StringBuilder → 超限前归还 | `Tools/ToolChatClient.cs` |
| A-78 | `MergeToolCallDelta` 丢无 Id 首块 → 无 Id 也创建条目 | `Tools/ToolChatClient.cs` |
| A-79 | `InvokeMethodAsync` 不支持 `ValueTask<T>` → 加 ValueTask 分支 | `Tools/ToolRegistry.cs` + `NativeToolTests.cs` |
| A-80 | `options.Tools` 无路由 → 按名路由 + 无路由警告 | `Tools/ToolChatClient.cs` |
| A-81 | `CodingTools` 正则 ReDoS → 2 秒匹配超时 | `Coding/CodingTools.cs` |
| A-82 | `TryInvokeAsync` 错误 JSON 未转义工具名 → 整体 ToJson | `Tools/ToolRegistry.cs` |
| A-83 | `WeatherNmcService` 全量扫描无锁 → 静态信号量 + 双检 | `Tools/WeatherNmcService.cs` |
| A-84 | ~~`AiClientRegistry` 工厂回写调用方 options → 克隆 options~~ → **误报**：直接引用 opts 为故意设计，已恢复 + 防回归注释 | `Clients/AiClientRegistry.cs` |
| A-85 | `BackgroundGenerationService` 旧任务误删新 CTS → ReferenceEquals 校验 | `Services/BackgroundGenerationService.cs` |
| A-86 | Ollama 拉模型 30 分钟超时被 HttpClient.Timeout 先触发 → 临时放大 + finally 恢复 | `Clients/Ollama/OllamaChatClient.cs` |
| A-87 | `AiClientBase` 5 处不释放响应 → using 释放 | `Clients/AiClientBase.cs` |
| A-88 | OpenAI/Anthropic/Gemini `/v1/v1` 双路径 → 统一 `CombineApiUrl`（正则支持 v1beta） | `Clients/OpenAIChatClient.cs`、`AnthropicChatClient.cs`、`GeminiChatClient.cs`、`AiClientBase.cs` + `AiClientBaseTests.cs` |
| A-89 | `deepseek-reasoner` 能力虚标 → SupportFunction 如实标注 | `Clients/OpenAI/DeepSeekChatClient.cs` |
| A-90 | Ollama 终结帧 FinishReason 覆写死代码 → 删除 | `Clients/Ollama/OllamaChatClient.cs` |
| A-91 | `NormalizeTextContent` 死分支 → 简化 | `Clients/DashScope/DashScopeRequest.cs` |
| A-92 | DashScope `DefaultEndpoint` setter no-op → 私有覆盖字段生效 | `Clients/DashScope/DashScopeChatClient.cs` |
| A-93 | Gemini 转换角色不一致 → 统一 assistant | `Clients/Gemini/GeminiResponse.cs` |

**验证**：`NAI/NewLife.AI.sln` 构建 0 错误；新增 5 个测试（过滤契约/状态重置/ValueTask×2/v1beta URL），全量回归见第 7 节。

---

## 6. 残留风险

| 风险 | 说明 | 缓解 |
|------|------|------|
| 渠道真实网络发送 | 11 渠道多为真实 HTTP 调用，无法单测覆盖发送成功路径 | 纯逻辑（配置校验/Markdown 转换）单测 + 错误路径 mock |
| A-31 MCP 同步阻塞 | `McpServer.Process` 同步公共 API，工具为 async Task 时内部同步等待 | 内存 body 读取不阻塞线程；保持同步签名；如需异步化需 API 变更 |
| A-71 实时多事件拼接 | 单条 WS 消息含多个 JSON 事件时解析失败被静默丢弃 | DashScope 实时协议通常一条消息一个事件；精细解析留待后续 |
| DNS 解析缓存 | `ToolHelper` DNS 缓存进程级，主机名 IP 变化需重启才刷新 | 默认 5 分钟内的短会话场景可接受；长时间运行可增加 TTL 失效 |

---

## 7. 分析结论（收敛）

经过 **5 轮深入审计**（2026-08-04 ~ 08-05），`NewLife.AI` 基础库 12 维度全部扫描完成，共登记 **94 个问题**（2 P0、10 P1、51 P2、31 P3），其中 **85 个已修复**、**4 个暂缓**、**5 个误报关闭**：

| 维度 | 问题数 | 已修 | 暂缓/关闭 |
|------|:------:|:----:|:--------:|
| D1 协议客户端残留 | 25 | 21 | A-03（安全做法）、A-31/71（见上）、A-94（协议请求消息一致性） |
| D2 工具调用引擎 | 14 | 14 | — |
| D3 IM 渠道 | 9 | 9 | — |
| D4 智能体框架 | 8 | 8 | — |
| D5 编程智能体 | 8 | 8 | — |
| D6 嵌入 | 2 | 2 | — |
| D7 过滤器 | 0 | — | — |
| D8 Handler 链 | 1 | 1 | — |
| D9 业务接口契约 | 1 | 1 | — |
| D10 记忆存储 | 0 | — | — |
| D11 MCP | 14 | 13 | A-41（扩展点保留） |
| D12 模型 DTO/服务/规划器 | 15 | 15 | — |

**验证基线**：`NAI/NewLife.AI.sln` 构建 0 错误；全量测试 **1500 通过 / 0 失败 / 7 跳过**（新增 5 个测试）。

**遗留说明**：暂缓项均为低风险或需公共 API / 架构级变更的权衡决策（A-94 需代理列表，A-31 需异步化签名，A-71 需精细 JSON 流解析）；核心链路（协议客户端、工具循环、渠道、MCP、Planner）健壮性已全面加固。NewLife.AI 库自身编译警告为 **0**。

---

（完）
