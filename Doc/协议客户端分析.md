# NewLife.AI 协议客户端分析

> 版本：v4.8 | 日期：2026-08-04
> 范围：`NAI/NewLife.AI/Clients/` 协议客户端部分的 **chat / stream / think** 基础对话与增强对话能力。工具（Tool）相关由独立分析覆盖，不在此文档展开。

---

## 1. 概述

本文档对 `NewLife.AI` 基础库的协议客户端部分做深入分析，目标是**确保所有支持的协议类型和模型服务商在 chat / stream / think 上稳定可靠使用**。

分析覆盖：
- 6 类协议实现（OpenAI Chat Completions、Anthropic Messages、Google Gemini、DashScope 原生、Ollama 原生、AWS Bedrock Converse）
- 37+ 个 OpenAI 兼容服务商（`BuiltinChatClient` 注册）
- 专用客户端（DeepSeek、AzureAI、NewLifeAI 多协议网关）

---

## 2. 协议客户端总览

### 2.1 架构

主体系为 **`AiClientBase` / `IChatClient` + `AiClientRegistry`**：

```text
AiClientRegistry（[AiClient] / [AiClientModel] 特性扫描注册）
  └─ AiClientDescriptor（无状态描述符 + Factory）
       └─ IChatClient 实例（AiClientBase 子类，绑定 Endpoint/ApiKey）
            ├─ GetResponseAsync          → 非流式
            └─ GetStreamingResponseAsync → 流式
                 └─ ChatStreamAsync（协议级 SSE/NDJSON 解析）
```

统一契约：
- `IChatRequest` / `IChatResponse`：跨协议统一请求/响应接口
- `ChatRequest` / `ChatResponse`：内部统一传输模型
- 各协议请求 DTO（`ChatCompletionRequest`/`AnthropicRequest`/`GeminiRequest`/`DashScopeRequest`/`OllamaChatRequest`/`BedrockRequest`）实现 `IChatRequest` 并实现 `FromChatRequest`/`ToChatRequest` 双向转换
- 各协议响应 DTO 实现 `IChatResponse`，统一暴露 `Messages`/`Usage`/`Text`

### 2.2 协议客户端清单

| 协议 | 客户端 | 基类 | 认证 | 端点形态 |
|------|--------|------|------|----------|
| OpenAI Chat Completions | `OpenAIClientBase` / `OpenAIChatClient` | `AiClientBase` | Bearer | `{endpoint}/v1/chat/completions` |
| OpenAI 兼容服务商（37+） | `BuiltinChatClient`（`partial OpenAIChatClient`） | `OpenAIChatClient` | Bearer | 各自 `ChatPath` 覆盖 |
| Anthropic Messages | `AnthropicChatClient` | `AiClientBase` | `x-api-key` + `anthropic-version` | `{endpoint}/v1/messages` |
| Google Gemini | `GeminiChatClient` | `AiClientBase` | URL `key` 参数 | `{endpoint}/v1/models/{model}:generateContent` |
| DashScope 原生 | `DashScopeChatClient`（原生模式） | `OpenAIChatClient` | Bearer | `{host}/api/v1/services/aigc/...` |
| DashScope 兼容 | `DashScopeChatClient`（兼容模式） | `OpenAIChatClient` | Bearer | `{host}/compatible-mode/v1/chat/completions` |
| Ollama 原生 | `OllamaChatClient` | `AiClientBase` | 可选 Bearer | `{endpoint}/api/chat`（NDJSON） |
| AWS Bedrock Converse | `BedrockChatClient` | `AiClientBase` | AWS SigV4 | `{region}.amazonaws.com/model/{id}/converse` |
| 深度求索 | `DeepSeekChatClient` | `OpenAIClientBase` | Bearer | `https://api.deepseek.com` |
| Azure OpenAI | `AzureAIChatClient` | `OpenAIClientBase` | `api-key` | `{endpoint}/openai/deployments/{model}/chat/completions` |
| 新生命 AI 网关 | `NewLifeAIChatClient` | `OpenAIChatClient` | Bearer | `https://ai.newlifex.com`（多协议端点） |

### 2.3 遗留体系清理（Phase 0）

原 `Providers/` 目录下 7 个旧 Provider 类（`IAiProvider`/`OpenAiProvider`/`AnthropicProvider`/`DashScopeProvider`/`GeminiProvider`/`NewLifeAiProvider`/`OllamaProvider`）为旧协议实现，已通过 `<Compile Remove>` 排除编译，运行时已全部改用 `AiClientRegistry`（`ModelService`/`GatewayService`/`模型配置.Biz`/`ModelPricingService` 均走 `GetDescriptor`）。经全工作区引用检查确认无任何代码依赖后，已于 2026-08-03 删除，并同步清理 `NewLife.AI.csproj` 的 `<Compile Remove>` 条目与 `IChatClient.AsBuilder` 的过时示例注释。

---

## 3. 协议矩阵（chat / stream / think）

| 协议 | 非流式 chat | 流式 stream | 思考 think 请求映射 | 思考 think 响应提取 | 用量统计 |
|------|:---:|:---:|------|------|------|
| OpenAI Chat Completions | ✅ | ✅ SSE | ⚠️ `enable_thinking`（Qwen 参数）+ `reasoning_effort` | ✅ `reasoning_content`/`reasoning` | ✅ 末 chunk 全量 |
| Anthropic Messages | ✅ | ✅ SSE（`event:`/`data:`） | ❌ 未映射（Phase 2 修复） | ✅ `thinking` block / `thinking_delta` | ⚠️ 流式 input 丢失（Phase 3 修复） |
| Google Gemini | ✅ | ✅ SSE | ❌ 未映射（Phase 2 修复） | ✅ `thought=true` part | ✅ 每 chunk 累积 |
| DashScope 原生 | ✅ | ✅ SSE（`event:`/`data:`） | ✅ `enable_thinking` + `thinking_budget` | ✅ `reasoning_content` | ✅ |
| DashScope 兼容 | ✅ | ✅ SSE | ✅ `enable_thinking` | ✅ `reasoning_content` | ✅ |
| Ollama 原生 | ✅ | ✅ NDJSON | ✅ `think` 参数 | ✅ `thinking` 字段 | ✅ |
| AWS Bedrock Converse | ✅ | ✅ SSE（`event:`/`data:`） | ❌ 未映射（Phase 2 修复，尽力） | ✅ `reasoningContent` | ✅ |

---

## 4. chat — 基础对话能力

### 4.1 统一请求构建

各协议通过 `FromChatRequest(IChatRequest)` 将统一请求转换为协议 DTO：
- `ChatCompletionRequest.FromChatRequest` / `BuildBody`：字典方式构建，仅含非空字段（避免 qwen-max 等严格校验拒绝 null），支持多模态 `Contents`（text/image/audio）与工具消息
- `AnthropicRequest.FromChatRequest`：`system` 消息提升为顶级字段、`stop`→`stop_sequences`、工具 `tool_use`/`tool_result` 内容块
- `GeminiRequest.FromChatRequest`：`contents` 数组、角色 `model`（非 assistant）、`systemInstruction` 顶级字段
- `DashScopeRequest.FromChatRequest`：`input.messages` 嵌套、`parameters` 嵌套、多模态内容块
- `OllamaChatRequest.FromChatRequest`：`messages` + `options`
- `BedrockRequest.FromChatRequest`：Converse `messages` + `inferenceConfig`

### 4.2 统一响应解析

各协议响应 DTO 实现 `IChatResponse`：
- `ChatCompletionResponse`：`choices[].message` / `delta`，`usage`（`prompt_tokens`/`completion_tokens`）
- `AnthropicResponse`：`content[]`（text/thinking/tool_use），`usage`
- `GeminiResponse`：`candidates[].content.parts[]`，`usageMetadata`
- `DashScopeResponse`：`output.choices[]`，`usage`
- `OllamaChatResponse`：`message` + `done`
- `BedrockResponse`：`output.message.content[]`，`usage`

### 4.3 健壮性

- ✅ 非法 assistant 消息跳过：`ShouldSkipAssistantMessage`（DeepSeek 等要求 assistant 至少含 content 或 tool_calls）
- ✅ 版本路径去重：`CombineApiUrl` 防止 `{endpoint}/v1` + `/v1/...` 产生双版本路径
- ✅ 错误响应：`PostAsync` 非 2xx 抛 `ApiException`（状态码 + 响应体）
- ✅ 空消息校验：`AiClientBase.GetResponseAsync`/`GetStreamingResponseAsync` 空消息抛 `ArgumentException`

---

## 5. stream — 流式对话能力

### 5.1 各协议流式格式

| 协议 | 流式格式 | SSE 前缀 | 终止标记 |
|------|----------|----------|----------|
| OpenAI 兼容 | SSE | `data: `（带空格） | `data: [DONE]` |
| Anthropic | SSE | `event:` + `data:`（无空格） | `message_stop` |
| Gemini | SSE | `data: `（带空格） | 流结束 |
| DashScope 原生 | SSE | `id:`/`event:`/`data:` | 流结束 |
| DashScope 兼容 | SSE | `data:`（无空格） | `[DONE]` |
| Ollama | NDJSON | 无前缀，逐行 JSON | `"done":true` |
| Bedrock | SSE | `event:` + `data:` | `messageStop` |

### 5.2 已修复缺口（Phase 3，2026-08-03）

| # | 缺口 | 影响 | 修复 | 状态 |
|---|------|------|------|:---:|
| S1 | **Anthropic 流式用量丢失**：`AiClientBase.GetStreamingResponseAsync` 只取最后一个非空 chunk 的 Usage；Anthropic 的 input/output token 分 `message_start`/`message_delta` 两个互补 chunk 返回，流式路径 `InputTokens` 恒为 0。下游 `MessageFlow`（Web）与 `GatewayService`（网关）同样取末 chunk，导致**用量记录/会话 Token 统计/计费错误**。`MergeChunkUsage` 覆写仅在 `ToolChatClient` 路径生效 | 计费与统计错误 | `GetStreamingResponseAsync` 改用 `MergeChunkUsage` 合并并回写 chunk.Usage | ✅ |
| S2 | **SSE 前缀不统一**：OpenAI 兼容要求 `data: `（带空格），DashScope 兼容用 `data:`；`[DONE]` 在 DashScope 兼容路径被静默跳过继续循环 | 部分服务商返回 `data:`（无空格）时整行被丢弃，流式中断 | 统一前缀兼容判断（忽略大小写）+ `[DONE]` 统一 break | ✅ |
| S3 | **流式错误静默吞掉**：OpenAI 兼容 / DashScope 兼容 / NewLifeAI 路径 `try { ParseChunk } catch { }` 静默吞异常；流式 `error` 对象被当作普通 chunk 丢弃 | 协议变更/服务商错误无感知，表现为静默截断 | 新增 `AiClientBase.EnsureNoStreamError` 识别流式 `error` 对象并抛异常；Anthropic/Bedrock 处理 `event:error`；Ollama 处理 NDJSON error | ✅ |

### 5.3 已健壮的部分

- ✅ 取消令牌：各协议流式循环均检查 `cancellationToken.ThrowIfCancellationRequested()`
- ✅ Anthropic 流式事件模型完整：`message_start`/`content_block_start`/`content_block_delta`（text/thinking）/`content_block_stop`/`message_delta`/`message_stop`
- ✅ Ollama 流式 tool_calls 语义：`done=false` 中间帧出现 tool_calls 时，终结帧覆写 `FinishReason=ToolCalls`，保证工具循环正确进入
- ✅ DashScope 原生流式错误：`event:error` 时解析 code/message 并抛 `HttpRequestException`

---

## 6. think — 思考模式能力

### 6.1 统一选项

- `ChatOptions.EnableThinking`（`Boolean?`）：null=不设置、true=开启、false=关闭
- `ChatOptions.ReasoningEffort`（`String?`）：推理强度（OpenAI o 系列 low/medium/high；DeepSeek high/max）
- 协议专属：`Items["ThinkingBudget"]`（DashScope 原生 `thinking_budget`）

### 6.2 各协议映射现状

| 协议 | EnableThinking → 协议字段 | ReasoningEffort | 状态 |
|------|---------------------------|-----------------|------|
| OpenAI 兼容 | `enable_thinking`（DashScope/Qwen 参数） | `reasoning_effort` | ⚠️ 兼容模式可用；OpenAI 原生不认识 `enable_thinking`（o 系列应走 `reasoning_effort`，忽略即可） |
| DeepSeek | `thinking: {type: "enabled"/"disabled"}` | `reasoning_effort` | ✅ |
| DashScope 原生 | `enable_thinking` + `thinking_budget` | — | ✅ |
| Ollama | `think` | — | ✅ |
| **Anthropic** | **未映射（`[IgnoreDataMember]`，静默丢弃）** | **未映射** | ❌ Phase 2 修复 |
| **Gemini** | **未映射** | **未映射** | ❌ Phase 2 修复 |
| **Bedrock** | **未映射** | **未映射** | ❌ Phase 2 修复（尽力） |

### 6.3 响应侧提取（已就绪）

| 协议 | 思考响应字段 | 提取位置 |
|------|-------------|----------|
| OpenAI 兼容 | `reasoning_content` / `reasoning` | `OpenAIClientBase.ParseChatMessage` → `ChatMessage.ReasoningContent` |
| Anthropic | `content[].type=thinking` / `thinking_delta` | `AnthropicResponse.ToChatResponse` / `ToChunkResponse` |
| Gemini | `part.thought=true` | `GeminiResponse` parts 解析 |
| DashScope | `reasoning_content` | 同上（OpenAI 兼容 + 原生适配） |
| Ollama | `message.thinking` | `OllamaChatResponse` → `ReasoningContent` |
| Bedrock | `content[].reasoningContent.reasoningText` | `BedrockResponse` |

### 6.4 已修复缺口（Phase 2，2026-08-03）

| # | 缺口 | 影响 | 修复 | 状态 |
|---|------|------|------|:---:|
| T1 | **Anthropic `EnableThinking`/`ReasoningEffort` 静默丢弃**：未映射到 `thinking: {type:"enabled", budget_tokens:N}`，Claude extended thinking 无法通过统一 API 控制 | 思考模式在 Anthropic 协议下不可用 | `AnthropicRequest` 新增 `Thinking` 字段，`EnableThinking` → `thinking:{type,budget_tokens}`（默认预算 1024，`ThinkingBudget` 可覆盖，max_tokens 自动兜底提升） | ✅ |
| T2 | **Gemini `EnableThinking`/`ReasoningEffort` 静默丢弃**：未映射到 `generationConfig.thinkingConfig.thinkingBudget`，Gemini 2.5 思考预算不可控 | 思考预算在 Gemini 协议下不可控 | `GeminiGenerationConfig` 新增 `ThinkingConfig`，`EnableThinking` → `thinkingConfig.thinkingBudget`（true=默认 1024，false=0 关闭） | ✅ |
| T3 | **Bedrock `EnableThinking` 静默丢弃**：Converse thinking 未映射（模型相关） | Claude-on-Bedrock 思考不可控 | `BedrockRequest` 新增 `AdditionalModelRequestFields`（官方支持字段），`EnableThinking` → 透传 Anthropic 格式 thinking（含 budget_tokens） | ✅ |

---

## 7. 测试覆盖现状

### 7.1 已有测试

- **DTO 序列化单元测试**（较全）：`ChatCompletionRequestTests`、`ChatCompletionResponseTests`、`AnthropicRequestTests`/`AnthropicResponseTests`、`GeminiRequestTests`/`GeminiResponseTests`、`BedrockChatClientTests`、`OllamaChatModelTests`、`AiClientBaseTests`（`CombineApiUrl`）
- **真实 Key 集成测试**（`RequiresApiKeyFact`，无 Key 跳过）：`DashScopeIntegrationTests`、`DeepSeekIntegrationTests`、`OpenAiChatClientIntegrationTests`（走 DashScope 兼容端点）、`OllamaIntegrationTests`、`NewLifeAiIntegrationTests`、`DashScopeOmniIntegrationTests`、`DashScopeTtsTests` 等
- **注册表测试**：`AiProviderTests`（46 个内置服务商注册）、`DashScopeAdvancedTests`

### 7.2 已补齐覆盖空白（Phase 4，2026-08-03）

| # | 空白 | 覆盖 | 状态 |
|---|------|------|:---:|
| C1 | **无 stub HttpClient / mock SSE 流协议级测试**：流式解析循环（SSE/NDJSON）无自动化验证 | 新增 `StubHttpMessageHandler`（可注入 `AiClientBase.HttpClient`）+ 六协议测试类（`*ChatClientStreamTests`），覆盖非流式/流式/思考/错误路径，无需真实 Key | ✅ |
| C2 | **think 映射无测试**：各协议 `EnableThinking` → 协议字段的序列化断言缺失 | 各测试类断言请求体序列化（`enable_thinking`/`thinking`/`thinkingConfig`/`think`/`additionalModelRequestFields`）；`AnthropicRequestTests` 更正错误断言并补 4 个 think 用例 | ✅ |
| C3 | **流式用量合并无验证**：Anthropic 分块 Usage 合并无验证 | `AnthropicChatClientStreamTests` 验证 message_start + message_delta → 合并后 input/output 完整 | ✅ |

---

## 8. 缺口清单与优先级

全部缺口已在本轮修复并补测试（2026-08-03）。

| 优先级 | 编码 | 缺口 | 模块 | 修复 | 状态 |
|:---:|------|------|------|------|:---:|
| P0 | S1 | Anthropic 流式用量丢失（计费错误） | stream | Phase 3 | ✅ |
| P0 | T1/T2 | Anthropic/Gemini think 参数静默丢弃 | think | Phase 2 | ✅ |
| P1 | S2 | SSE 前缀与 `[DONE]` 处理不统一 | stream | Phase 3 | ✅ |
| P1 | S3 | 流式错误静默吞掉 | stream | Phase 3 | ✅ |
| P1 | T3 | Bedrock think 参数未映射 | think | Phase 2 | ✅ |
| P1 | C1/C2/C3 | mock 协议测试 + think/usage 测试缺失 | 测试 | Phase 4 | ✅ |

### 8.1 第二轮独立复核缺口（v2.0，2026-08-03）

用户要求**重新完整深入分析**（不信任首轮成果）。独立复核确认首轮修复（S1/S2/S3/T1/T2/T3）全部属实，并新发现以下缺口：

| 优先级 | 编码 | 缺口 | 模块 | 修复 | 状态 |
|:---:|------|------|------|------|:---:|
| P1 | G1 | 流式块解析 `catch {}` 仍静默吞掉**非 error 类**畸形块（OpenAI/NewLifeAI/DashScope 共 7 处），协议漂移时表现为静默截断难排查 | stream | Phase 6 | ✅ |
| P1 | G2 | `NewLifeAiChatClient` 三路流式（OpenAI/Anthropic/Gemini）无 stub 协议测试 | 测试 | Phase 9 | ✅ |
| P1 | G3 | SDK 层无 429/5xx 重试（网关层 `GatewayService` 有，Web/`MessageFlow` 与 SDK 直用路径没有） | chat/stream | Phase 7 | ✅ |
| P2 | G4 | 46 个服务商注册元数据（endpoint/路径/模型）无自动校验 | 测试 | Phase 9 | ✅ |
| P1 | N1 | **Gemini `ResponseFormat` 未映射**：OpenAI 风格 `{type:"json_object"/"json_schema"}` 未转 `generationConfig.responseMimeType/responseSchema`（官方原生支持） | think/增强 | Phase 8 | ✅ |
| P2 | N2 | **Ollama `TopK` 未映射**：`OllamaChatRequest` 有 TopK 属性但未接 `IChatRequest.TopK` | 增强 | Phase 8 | ✅ |
| P2 | N3 | Gemini/NewLifeAI `ChatStreamViaPathAsync` 流式仅认 `data: `（带空格），未统一容忍 `data:`（无空格） | stream | Phase 8 | ✅ |
| P2 | N4 | **Gemini `PresencePenalty`/`FrequencyPenalty` 未映射**（官方 `generationConfig` 原生支持） | 增强 | Phase 8 | ✅ |

### 8.2 第三轮独立复核缺口（v3.0，2026-08-03）

用户要求**第三轮完整深入分析**（独立复核前两轮成果）。确认前两轮修复（S1/S2/S3/T1/T2/T3/G1/G3/N1-N4）全部属实，并聚焦「多轮对话 × 思考模式」这一前两轮未覆盖的维度，新发现以下缺口：

| 优先级 | 编码 | 缺口 | 模块 | 修复 | 状态 |
|:---:|------|------|------|------|:---:|
| P0 | A1 | **Anthropic 多轮思考回传缺失**：`AnthropicContentBlock` 无 `Signature` 字段，流式 `signature_delta` 事件未处理，`AnthropicRequest.FromChatRequest` 不回传 assistant 思考块 | think | Phase 11 | ✅ |
| P0 | A2 | **`redacted_thinking` 块未建模**：非流式 `data` 字段与流式 `content_block_start` 均未捕获，工具轮次回传被静默丢弃 | think | Phase 11 | ✅ |
| P1 | A3 | **Anthropic 思考与采样参数冲突**：思考开启时仍下发 temperature/top_k（老模型 400）、top_p 越界 | think | Phase 12 | ✅ |
| P1 | A4 | **Anthropic adaptive 思考前向兼容缺失**：4.6+ 模型 `enabled` 已弃用、4.7+ 直接 400；`ReasoningEffort` 未映射 | think | Phase 12 | ✅ |
| P1 | A5 | **Ollama/DashScope 多轮思考回放缺失**：`ReasoningContent` 未回传到协议 thinking / reasoning_content 字段 | think | Phase 13 | ✅ |
| P2 | A6 | **`ThinkingBudget` 无边界约束**：Anthropic 最小 1024，低于该值 API 拒绝 | think | Phase 12 | ✅ |

### 8.3 第四轮独立复核缺口（v4.0，2026-08-03）

用户要求**第四轮完整深入分析**（独立复核前三轮成果）。确认前三轮修复全部属实，并聚焦「全协议一致性盲区」，新发现以下缺口：

| 优先级 | 编码 | 缺口 | 模块 | 修复 | 状态 |
|:---:|------|------|------|------|:---:|
| P1 | R4-1 | **Gemini 原生流式缺错误识别与容错**：`ChatStreamAsync` 未调用 `EnsureNoStreamError`（流式 `{"error":{...}}` 被反序列化成空 chunk 静默吞掉），也缺 `try/catch + LogParseChunkError`（畸形块直接抛异常中断整个流）。其余 5 协议均已统一，仅 Gemini 遗漏 | stream | Phase 15 | ✅ |
| P1 | R4-2 | **OpenAI 推理模型 `max_tokens` 不兼容**：注册表已有 `o3-mini`/`o4-mini`/`gpt-5-mini`，这些模型强制要求 `max_completion_tokens`，当前始终输出 `max_tokens` 会被 OpenAI 拒绝（400） | chat | Phase 15 | ✅ |
| P1 | R4-3 | **Bedrock 自定义区域端点与签名区域错位**：`GetRegionEndpoint()` 用 `Contains("us-east-1")` 判断自定义端点，但 SigV4 签名始终用 `Region`（=Protocol，默认 us-east-1）。配置非 us-east-1 区域端点而未设 Protocol 时请求发往目标区域却用 us-east-1 签名 → 403 | chat | Phase 15 | ✅ |
| P2 | R4-4 | **Anthropic/Gemini 多 system 消息静默覆盖**：`FromChatRequest` 循环中 `System`/`SystemInstruction` 覆盖，多条 system 仅保留最后一条 | chat/think | Phase 15 | ✅ |
| P2 | R4-5 | **Ollama `ParseChunk` 残留死代码**：空局部变量 `var tc = resp.Message.ToolCalls[0];` | 代码卫生 | Phase 15 | ✅ |
| P3 | R4-6 | **`ChatCompletionRequest.FromChatRequest` 未透传 `Items`**：与 `ToChatRequest`（透传 Items）不对称，直传 DTO 场景下扩展元数据丢失 | 增强参数 | Phase 15 | ✅ |

### 8.4 第五轮独立复核缺口（v4.1，2026-08-03）

用户要求**第五轮完整深入分析**。确认前四轮修复全部属实，并聚焦「用量明细映射」这一协议矩阵标 ✅ 但明细缺失的维度，新发现以下缺口：

| 优先级 | 编码 | 缺口 | 模块 | 修复 | 状态 |
|:---:|------|------|------|------|:---:|
| P1 | R5-1 | **reasoning tokens 跨协议映射缺失**：`UsageDetails.ReasoningTokens` 存在且 `UsageService` 写入 `UsageRecord.ReasoningTokens`，但 OpenAI（`completion_tokens_details.reasoning_tokens`）/ Gemini（`thoughtsTokenCount`）/ DashScope（`thinking_tokens`）均未映射 → 思考模式推理 Token 计费恒 0 | 用量 | Phase 16 | ✅ |
| P2 | R5-2 | **cache tokens 映射缺失（Gemini/Anthropic）**：Gemini `cachedContentTokenCount`、Anthropic `cache_read_input_tokens`/`cache_creation_input_tokens` 未映射 → 缓存命中/创建计费缺失（OpenAI/DashScope 已映射） | 用量 | Phase 16 | ✅ |
| P3 | R5-3 | **OpenAI Responses API 声明与实现不符**：`ChatCompletionResponse` 注释声称兼容 v1/responses 但无 Responses output 解析，实为 NewLifeAI 网关归一化 | 文档 | Phase 16 | ✅ |
| P3 | R5-4 | **Gemini 非流式 HTTP 200 错误体未识别**：部分拦截场景以 HTTP 200 + `{"error":{...}}` 返回，`ParseResponse` 未识别 → 空响应 | 健壮性 | Phase 16 | ✅ |

### 8.5 第六轮独立复核缺口（v4.2，2026-08-04）

用户要求**第六轮完整深入分析**。确认前五轮修复全部属实，并验证第五轮用量映射在**流式路径**的真实性，新发现以下缺口：

| 优先级 | 编码 | 缺口 | 模块 | 修复 | 状态 |
|:---:|------|------|------|------|:---:|
| P1 | R6-1 | **Anthropic 流式 cache tokens 丢失**：第五轮将 cache 映射补到非流式适配器，但流式 `ToChunkResponse` 的 `message_start`/`message_delta` 仍只映射 input/output → Web 对话默认流式，缓存命中/创建计费在流式下依然缺失 | 用量/stream | Phase 17 | ✅ |
| P3 | R6-2 | **Gemini finishReason 覆盖不全**：`MapGeminiFinishReason` 仅映射 SAFETY/RECITATION，BLOCKLIST/PROHIBITED_CONTENT/SPII 等安全拦截原因返回 null → 前端无法区分拦截与正常停止 | 健壮性 | Phase 17 | ✅ |

### 8.6 第七轮独立复核缺口（v4.3，2026-08-04）

用户要求**第七轮完整深入分析**。确认前六轮修复全部属实，并聚焦「多模态输入跨协议一致性」，新发现以下缺口：

| 优先级 | 编码 | 缺口 | 模块 | 修复 | 状态 |
|:---:|------|------|------|------|:---:|
| P1 | R7-1 | **非 OpenAI 协议多模态输入未转换**：`Contents` 中的 `ImageContent`/`AudioContent` 仅在 OpenAI/DashScope 转换，Anthropic/Gemini/Ollama/Bedrock 的 `FromChatRequest` 均只读 `msg.Content` → 图片/音频消息静默丢失或错误格式序列化 | 多模态/chat | Phase 18 | ✅ |
| P3 | R7-2 | **`ChatCompletionRequest.ToChatRequest` 反向转换丢字段**：`ReasoningEffort`/`UserId`/`ConversationId` 未复制，round-trip 数据丢失（与 FromChatRequest 不对称） | 健壮性 | Phase 18 | ✅ |

### 8.7 第八轮独立复核缺口（v4.4，2026-08-04）

用户要求**第八轮完整深入分析**。确认前七轮修复全部属实，并聚焦「生成参数映射的残余盲区」，新发现以下缺口：

| 优先级 | 编码 | 缺口 | 模块 | 修复 | 状态 |
|:---:|------|------|------|------|:---:|
| P2 | R8-1 | **Ollama 结构化输出未映射**：`OllamaChatRequest.ResponseFormat` 为 `[IgnoreDataMember]` 且未转 `format` 字段 → 到 Ollama 的 JSON 模式/结构化输出静默丢弃 | 增强 | Phase 19 | ✅ |
| P2 | R8-2 | **Ollama 惩罚/确定性参数未透传**：`OllamaOptions` 已有 `Seed`/`RepeatPenalty`/`PresencePenalty`/`FrequencyPenalty` 字段但 `FromChatRequest` 未填充 | 增强 | Phase 19 | ✅ |
| P3 | R8-3 | **OpenAI `seed`/`logprobs`/`top_logprobs` Items 未透传**：DashScope 经 `Items["Seed"]` 等映射，OpenAI 的 `BuildBody`/`FromChatRequest` 未映射（Gemini `seed` 同步缺失） | 增强 | Phase 19 | ✅ |

### 8.8 第九轮独立复核缺口（v4.5，2026-08-04）

用户要求**第九轮完整深入分析**。确认前八轮修复全部属实，并聚焦「协议专属能力参数」这一更深入维度，新发现以下缺口：

| 优先级 | 编码 | 缺口 | 模块 | 修复 | 状态 |
|:---:|------|------|------|------|:---:|
| P2 | R9-1 | **Gemini `safetySettings` 未映射**：Gemini 原生安全设置（按类别/阈值阻断有害内容）为 0 匹配，企业场景硬需求缺失 | 增强 | Phase 20 | ✅ |
| P2 | R9-2 | **DashScope `VlHighResolutionImages`/`MaxPixels` 未映射**：`SendMessageRequest.Options` 文档声明支持但 SDK 未实现，高分辨率视觉输入参数静默丢弃 | 增强 | Phase 20 | ✅ |
| P3 | R9-3 | **Bedrock `TopK`/`PresencePenalty`/`FrequencyPenalty` 未映射**：`BedrockInferenceConfig` 无这些字段，Converse API 原生支持 | 增强 | Phase 20 | ✅ |

### 8.9 第十轮独立复核缺口（v4.6，2026-08-04）

用户要求**第十轮完整深入分析**。确认前九轮修复全部属实，并处理剩余的实际可用性缺口，新发现以下缺口：

| 优先级 | 编码 | 缺口 | 模块 | 修复 | 状态 |
|:---:|------|------|------|------|:---:|
| P2 | R10-1 | **Ollama `NumCtx`/`KeepAlive` 未映射**：本地模型默认上下文窗口较小（2048），长对话易截断且无法配置驻留时长 | 增强 | Phase 21 | ✅ |
| P3 | R10-2 | **Anthropic `User` 未映射 `metadata.user_id`**：`IChatRequest.User` 存在但未发送到 Anthropic，滥用追踪/用量归属缺失 | 增强 | Phase 21 | ✅ |

### 8.10 第十一轮独立复核缺口（v4.7，2026-08-04）

用户要求**第十一轮完整深入分析**。确认前十轮修复全部属实，并转向此前分析较少的**响应侧一致性**维度，新发现以下缺口：

| 优先级 | 编码 | 缺口 | 模块 | 修复 | 状态 |
|:---:|------|------|------|------|:---:|
| P2 | R11-1 | **`FilteredChatClient` 流式聚合丢思考/结束原因**：`OnStreamCompletedAsync` 的 `context.Response` 仅聚合 `Delta.Content`，`ReasoningContent`/`FinishReason` 丢失 → 学习/蒸馏类过滤器读到的完成响应缺思考内容与结束原因 | think/stream | Phase 22 | ✅ |
| P3 | R11-2 | **`ChatCompletionResponse.Text` 多模态数组返回垃圾值**：内容为数组时返回 `ToString()`（如 List 类型名），`Text` 提取不健壮 | 健壮性 | Phase 22 | ✅ |

### 8.11 第十二轮独立复核缺口（v4.8，2026-08-04）

用户要求**第十二轮完整深入分析**。本轮做系统性收尾审计（能力接口实现一致性 + 转换路径一致性），新发现以下缺口：

| 优先级 | 编码 | 缺口 | 模块 | 修复 | 状态 |
|:---:|------|------|------|------|:---:|
| P3 | R12-1 | **Ollama `ToChatResponse`/`ToStreamChunk` finish_reason 与 getter 不一致**：`IChatResponse.Messages` getter 已映射 done 缺省 Stop + tool_calls→ToolCalls，但两个转换方法缺失 → 公共转换 API 在工具场景返回错误结束原因 | 一致性 | Phase 23 | ✅ |

---

## 9. 修复记录

### Phase 0（2026-08-03）— 清理旧 Providers 死代码
- 删除 `NAI/NewLife.AI/Providers/` 全部 7 个旧 Provider 文件（已排除编译、无代码引用）
- 清理 `NewLife.AI.csproj` 的 `<Compile Remove>` 条目
- 修正 `IChatClient.AsBuilder` 过时示例注释（`DashScopeProvider` → `AiClientRegistry`）

### Phase 1（2026-08-03）— 分析报告
- 产出本文档：协议矩阵、chat/stream/think 现状与缺口表、测试覆盖、残留风险

### Phase 2（2026-08-03）— think 缺口修复
- `AnthropicRequest`：新增 `Thinking`/`AnthropicThinkingConfig`，`EnableThinking` → `thinking:{type,budget_tokens}`（默认预算 1024，`ThinkingBudget` 覆盖，max_tokens 自动兜底）
- `GeminiRequest`：`GeminiGenerationConfig` 新增 `ThinkingConfig`/`GeminiThinkingConfig`，`EnableThinking` → `thinkingConfig.thinkingBudget`
- `BedrockRequest`：新增 `AdditionalModelRequestFields`，`EnableThinking` → 透传 Anthropic 格式 thinking（官方支持的 `additionalModelRequestFields` 字段）
- 更正 `AnthropicRequestTests.FromChatRequest_Thinking` 错误断言，补 4 个 think 映射用例

### Phase 3（2026-08-03）— stream 健壮性修复
- S1：`AiClientBase.GetStreamingResponseAsync` 用 `MergeChunkUsage` 合并并回写 chunk.Usage，Anthropic 流式用量完整（下游 MessageFlow/GatewayService 一并受益）
- S2：SSE 前缀统一为 `data:`（忽略大小写）+ `[DONE]` 统一 break（OpenAI 兼容 / DashScope 兼容 ×2 / NewLifeAI ×2）
- S3：新增 `AiClientBase.EnsureNoStreamError`，OpenAI 兼容路径识别流式 error 对象并抛异常；Anthropic/Bedrock 处理 `event:error`；Ollama 处理 NDJSON error

### Phase 4（2026-08-03）— mock 测试基建 + 协议级测试
- 新增 `XUnitTest/Helpers/StubHttpMessageHandler.cs`（可注入 `AiClientBase.HttpClient`，支持 JSON/SSE/NDJSON 预制响应 + 请求头捕获）
- 新增 6 个协议级测试类（42 用例）：OpenAI（SSE/`[DONE]`/reasoning/error）、Anthropic（event/data/thinking_delta/usage 合并/error）、Gemini（thought=true/thinkingConfig）、DashScope 原生（SSE/error）、Ollama（NDJSON/think/error）、Bedrock（SigV4/reasoning/嵌套 SSE/error）
- 修复 `GeminiResponse.Text` 跳过 thought part（正文不应含思考内容）

### Phase 5（2026-08-03）— 验证与回归
- `NAI/NewLife.AI.sln` 构建 0 错误；全量测试 **1355 通过 / 0 失败 / 7 跳过**（跳过为未配置 Key 的集成测试）
- **S3 修复暴露既有测试假通过**：`DashScopeOmniIntegrationTests.OmniStream_AudioModality` 使用不支持的音色 `Cherry`（服务商错误被静默吞掉一直假通过），`EnsureNoStreamError` 正确抛出后，按官方文档改为默认音色 `Tina` 后真实通过
- `星语.sln` 全量构建因运行中的宿主应用进程（PID 11300）锁定 `NewLife.AI.dll` 未执行；改动全部在 `NAI/` 内，NAI 解决方案已覆盖验证

### Phase 6（2026-08-03）— G1 流式块解析容错可见化
- 新增 `AiClientBase.LogParseChunkError`：畸形数据块 WARN 日志（截断 200 字符）+ 跳过，保持容错同时让协议漂移可见
- 替换 7 处 `catch { }` 静默吞异常：`OpenAIClientBase.ChatStreamAsync`、`NewLifeAiChatClient`（Anthropic/Gemini/OpenAI 三路）、`DashScopeChatClient`（兼容/Omni/原生三路）
- 新增 `AiClientParseToleranceTests`（3 用例）：畸形块夹在有效块间跳过不中断、非 chunk 结构心跳跳过、仅畸形块返回空流不抛异常

### Phase 7（2026-08-03）— G3 SDK 层重试机制
- `AiClientOptions` 新增 `RetryCount`（默认 0=关闭）/`RetryIntervalMs`（默认 1000），`AiClientBase` 构造同步读取 Timeout 的既有模式不破坏
- `PostAsync`/`PostStreamAsync` 重构为「重试包装 + 单次发送」：`ShouldRetry` 仅对 429 限流、5xx 服务端错误、网络异常（含超时，非用户取消）重试，4xx 客户端错误不重试
- 指数退避：基础间隔 × 2^重试序号，上限 30 秒；流式仅在首字节前重试（数据未消费可安全重试）
- 新增 `AiClientRetryTests`（6 用例）：429/500 后重试成功、400 不重试、默认关闭、重试耗尽、流式 429 重试

### Phase 8（2026-08-03）— 增强参数映射补齐（N1/N2/N3/N4）
- **N1 Gemini `ResponseFormat`**：新增 `ApplyResponseFormat` 将 OpenAI 风格 `{type:"json_object"/"json_schema"}` 映射为 `generationConfig.responseMimeType="application/json"` + `responseSchema`（兼容 Dictionary/JsonElement/JSON 字符串）；对照官方 generate-content 文档核验
- **N4 Gemini `PresencePenalty`/`FrequencyPenalty`**：映射到 `generationConfig`（官方原生支持，对照文档确认）
- **N2 Ollama `TopK`**：`OllamaChatRequest` 补 `IChatRequest.TopK` 显式实现 + `FromChatRequest` 写入 `options.top_k`
- **N3 SSE 前缀统一**：`GeminiChatClient` 与 `NewLifeAiChatClient.ChatStreamViaPathAsync` 由 `data: `（带空格）放宽为 `data:`（忽略大小写），与 Phase 3 S2 一致
- 新增 `ChatOptionsMappingTests`（8 用例）：Gemini ResponseFormat 三种表示、presence/frequency 惩罚、无参不生成 config；Ollama TopK、无参不生成 options

### Phase 9（2026-08-03）— G2/G4 测试加固
- **G2 NewLifeAI 三路流式 stub 测试**：新增 `NewLifeAiChatClientStreamTests`（9 用例），复用 `StubHttpMessageHandler` 覆盖 `/v1/chat/completions`（SSE/无空格前缀/error）、`/v1/messages`（text/thinking_delta 提取/非流式）、`/v1/gemini`（thought part 跳过/非流式/error）
- **G4 服务商元数据校验**：新增 `ProviderMetadataTests`（7 用例），校验 46 个描述符的 Code/DisplayName 非空、endpoint 合法（允许 `{resource}` 模板占位符）、注册模型标识非空、工厂可创建客户端、关键服务商 ChatPath 覆盖与默认端点

### Phase 10（2026-08-03）— 验证与回归
- `NAI/NewLife.AI.sln` 构建 0 错误；全量测试 **1389 通过 / 0 失败 / 7 跳过**（跳过为未配置 Key 的集成测试；较首轮 +34 新用例）
- `星语.sln` 编译阶段通过，仅 DLL 复制因运行中的 dotnet 进程（PID 11300）锁定 `NewLife.AI.dll` 失败（MSB3027/MSB3021），与首轮一致，非代码问题

### Phase 11（2026-08-03）— Anthropic 多轮思考回传（A1/A2）
- `AnthropicContentBlock` 新增 `Signature`（thinking 签名）与 `Data`（redacted_thinking 加密数据）字段
- `AnthropicDelta` 新增 `Signature`；`AnthropicStreamEvent.ToChunkResponse` 处理流式 `signature_delta` 事件与 `content_block_start`（redacted_thinking 块）
- `AnthropicResponse` 新增 `ExtractThinkingMeta`：非流式适配与 `ToChatResponse` 将签名/redacted 数据透传到 `ChatMessage.Items`
- `ChatMessage` 新增 `Items` 扩展元数据（`[IgnoreDataMember]`，与请求/响应既有模式一致）
- `AnthropicRequest.FromChatRequest`：assistant 消息按官方块顺序回传 redacted_thinking + thinking（含签名）+ text + tool_use
- `ToolChatClient` 非流式/流式两路径透传 `Items`（流式路径累积 signature_delta 与 redacted_thinking 数据）

### Phase 12（2026-08-03）— Anthropic adaptive + 采样剥离 + budget clamp（A3/A4/A6）
- `AnthropicThinkingConfig` 支持 `adaptive` 模式与 `Display`（summarized/omitted）；`Items["ThinkingMode"]="adaptive"` 切换，`Items["ThinkingDisplay"]` 控制展示
- `ReasoningEffort` → `output_config.effort`（新增 `AnthropicOutputConfig`，仅 adaptive 模式下发）
- 思考开启时自动剥离 `temperature`/`top_k`、收敛 `top_p` 到 0.95~1（规避老模型 400）
- `ThinkingBudget` 最小 1024 clamp（官方约束：小于该值 API 拒绝）

### Phase 13（2026-08-03）— Ollama/DashScope 多轮思考回放（A5）
- `OllamaChatRequest.FromChatRequest`：`ReasoningContent` 回填 `OllamaChatMessage.Thinking`（Ollama 原生思考字段）
- `DashScopeRequest.BuildMessages`：`ReasoningContent` 回填 `DashScopeMessage.ReasoningContent`（Qwen 多轮思考）

### Phase 14（2026-08-03）— 测试与回归
- 新增协议级测试 **17 用例**：AnthropicRequestTests（多轮思考/签名/redacted/adaptive/采样剥离/budget clamp 7 用例）、AnthropicResponseTests（签名/redacted 提取 4 用例）、AnthropicChatClientStreamTests（signature_delta/redacted 流式 2 用例）、OllamaChatModelTests（thinking 回放 1 用例）、DashScopeRequestTests（reasoning_content 回放 1 用例）、ToolChatClientThinkRoundTripTests（非流式/流式签名透传 2 用例）
- 修复过程中发现并修正：`msg.Items["Signature"]` 直接字典索引在键缺失时抛 `KeyNotFoundException`（改用 `ChatMessage` 索引器 TryGetValue 语义）；thinking 块在无思考内容时被误添加
- `NAI/NewLife.AI.sln` 构建 0 错误；全量测试 **1406 通过 / 0 失败 / 7 跳过**（较第二轮 +17 新用例）

### Phase 15（2026-08-03）— 第四轮全协议一致性修复（R4-1~R4-6）
- **R4-1 Gemini 流式健壮性**：`GeminiChatClient.ChatStreamAsync` 补 `EnsureNoStreamError`（流式 error 对象抛 `HttpRequestException`，不再静默吞掉）+ `try/catch { LogParseChunkError }` 畸形块容错跳过，与 OpenAI/NewLifeAI/DashScope 路径对齐
- **R4-2 OpenAI `max_completion_tokens`**：`ChatCompletionRequest` 新增 `MaxCompletionTokens` 属性（序列化 `max_completion_tokens`）与 `IsMaxCompletionTokensModel` 判断（`o1`/`o3`/`o4`/`gpt-5` 前缀）；`FromChatRequest`/`BuildBody` 按模型自动选择字段，`ToChatRequest` 往返合并回 `MaxTokens`；DeepSeek/DashScope 走 `BuildBody` 字典路径不受影响（其模型仍用 `max_tokens`）
- **R4-3 Bedrock 区域推导**：`Region` 属性在 `Protocol` 未设置时从 Endpoint host 正则推导（`bedrock-runtime.{region}.amazonaws.com`）；`GetRegionEndpoint` 改为「显式 Endpoint 优先，否则按 Region 构造」，消除 `Contains("us-east-1")` 脆弱判断，保证自定义区域端点时 SigV4 签名区域与请求区域一致
- **R4-4 多 system 消息合并**：`AnthropicRequest.FromChatRequest`/`GeminiRequest.FromChatRequest` 将多条 system 消息按顺序以 `\n\n` 合并，不再静默覆盖
- **R4-5 死代码清理**：`OllamaChatClient.ParseChunk` 删除空局部变量块
- **R4-6 Items 透传**：`ChatCompletionRequest.FromChatRequest` 透传 `ChatOptions.Items` 扩展数据，与 `ToChatRequest` 对称
- 新增协议级测试 **10 用例**：GeminiChatClientStreamTests（流式 error 抛出 + 畸形块跳过 2 用例）、ChatCompletionRequestTests（o3/gpt-5 max_completion_tokens 映射、BuildBody 字段、Items 透传 4 用例）、BedrockChatClientTests（自定义端点区域推导 + 签名区域一致 2 用例）、AnthropicRequestTests / GeminiRequestTests（多 system 合并各 1 用例）
- `NAI/NewLife.AI.sln` 构建 0 错误；全量测试 **1420 通过 / 0 失败 / 7 跳过**（较第三轮 +14 新用例）

### Phase 16（2026-08-03）— 第五轮用量明细映射修复（R5-1~R5-4）
- **R5-1 reasoning tokens 映射**：
  - OpenAI：`CompletionUsage` 新增 `CompletionTokensDetails`（含 `ReasoningTokens`），`IChatResponse.Usage` 适配与 `ToChatResponse` 映射 `completion_tokens_details.reasoning_tokens`；`From` 仅在 >0 时输出（避免向不支持的服务商下发 0 值字段）
  - Gemini：`GeminiUsageMetadata` 新增 `ThoughtsTokenCount`，两处用量适配映射 `thoughtsTokenCount`
  - DashScope：`DashScopeUsageData` 新增 `ThinkingTokens`，`ToUsageDetails` 映射 `thinking_tokens`
- **R5-2 cache tokens 映射**：Gemini `GeminiUsageMetadata` 新增 `CachedContentTokenCount` → `CachedInputTokens`；Anthropic `AnthropicUsage` 新增 `CacheCreationInputTokens`/`CacheReadInputTokens` → `CacheCreationTokens`/`CachedInputTokens`（OpenAI/DashScope 原有映射不动）
- **R5-3 文档澄清**：`ChatCompletionResponse` 注释修正为「兼容 v1/chat/completions 协议（v1/responses 由 NewLifeAI 网关归一化后同样适用）」
- **R5-4 Gemini 非流式错误识别**：`GeminiChatClient.ParseResponse` 补 `EnsureNoStreamError`，HTTP 200 + 错误体不再静默返回空响应
- 新增协议级测试 **5 用例**：ChatCompletionResponseTests（reasoning_tokens 映射）、GeminiResponseTests（thoughts/cached 映射）、AnthropicResponseTests（cache_read/creation 映射）、DashScopeAdvancedTests（thinking_tokens 映射）、GeminiChatClientStreamTests（非流式 error 抛出）
- `NAI/NewLife.AI.sln` 构建 0 错误；全量测试 **1425 通过 / 0 失败 / 7 跳过**（较第四轮 +5 新用例）

### Phase 17（2026-08-04）— 第六轮流式用量/健壮性修复（R6-1/R6-2）
- **R6-1 Anthropic 流式 cache tokens 映射**：`AnthropicStreamEvent.ToChunkResponse` 的 `message_start` 补 `cache_read_input_tokens`/`cache_creation_input_tokens`（Anthropic 在 message_start 下发输入侧用量），`message_delta` 同样补映射（部分 API 版本在 message_delta 下发缓存用量）；与 `MergeChunkUsage` 局部填充合并，流式末 chunk 缓存用量完整
- **R6-2 Gemini finishReason 补全**：`MapGeminiFinishReason` 新增 `BLOCKLIST`/`PROHIBITED_CONTENT`/`SPII` → `FinishReason.ContentFilter`（版权/禁止内容/敏感个人信息拦截）
- 新增协议级测试 **2 用例**：AnthropicChatClientStreamTests（流式 cache_read/cache_creation 随 message_start/message_delta 合并完整）、GeminiResponseTests（5 类安全拦截 finishReason 均映射 ContentFilter）
- `NAI/NewLife.AI.sln` 构建 0 错误；全量测试 **1427 通过 / 0 失败 / 7 跳过**（较第五轮 +2 新用例）

### Phase 18（2026-08-04）— 第七轮多模态输入转换修复（R7-1/R7-2）
- **R7-1 四协议多模态输入转换**：新增共享辅助 `Models/AIContentHelper`（data URI 解析 + 媒体类型格式提取），各协议 `FromChatRequest` 转换 `Contents`：
  - **Gemini**：`GeminiPart` 新增 `InlineData`/`FileData`（`GeminiInlineData`/`GeminiFileData`），`ImageContent`/`AudioContent` → `inlineData`（base64 / data URI），外部 URL → `fileData`
  - **Anthropic**：用户消息含图片时构建 `image` 内容块（base64 源 / URL 源），文本与图片混合保持块顺序
  - **Ollama**：`OllamaChatMessage` 新增 `Images`（base64 数组），图片 → `images` 字段，文本部分合并到 `Content`
  - **Bedrock**：`BedrockContentBlock` 新增 `Image`（`BedrockImage`/`BedrockImageSource`），图片 → Converse `image` 内容块
- **R7-2 反向转换字段补齐**：`ChatCompletionRequest.ToChatRequest` 补 `ReasoningEffort`/`UserId`/`ConversationId`
- 新增协议级测试 **7 用例**：GeminiRequestTests（ImageContent→inlineData、data URI→inlineData）、OllamaChatModelTests（ImageContent→images、data URI→images）、AnthropicRequestTests（ImageContent→image 块）、BedrockChatClientTests（ImageContent→image 块）、ChatCompletionRequestTests（ToChatRequest 字段保留）
- `NAI/NewLife.AI.sln` 构建 0 错误；全量测试 **1434 通过 / 0 失败 / 7 跳过**（较第六轮 +7 新用例）

### Phase 19（2026-08-04）— 第八轮生成参数映射修复（R8-1/R8-2/R8-3）
- **R8-1 Ollama 结构化输出**：`OllamaChatRequest` 新增 `Format` 属性（序列化 `format`），`FromChatRequest` 经 `MapResponseFormat` 将 `ResponseFormat` → `"json"`（json_object）或 schema 对象（json_schema），写法与 Gemini `ApplyResponseFormat` 对齐
- **R8-2 Ollama 生成参数**：`FromChatRequest` 补 `PresencePenalty`/`FrequencyPenalty` 直映射 + `Items["Seed"]`/`Items["RepetitionPenalty"]` 透传（Items 键名与 DashScope 约定一致），`hasOptions` 判断同步扩展
- **R8-3 OpenAI/Gemini seed 透传**：`ChatCompletionRequest` 新增 `Seed`/`Logprobs`/`TopLogprobs` 属性，`FromChatRequest`/`BuildBody` 经 `Items` 映射；`GeminiGenerationConfig` 新增 `Seed`，`FromChatRequest` 映射
- 新增协议级测试 **4 用例**：OllamaChatModelTests（ResponseFormat→format、Seed/惩罚→options）、ChatCompletionRequestTests（Seed/Logprobs/TopLogprobs→属性与 BuildBody）、GeminiRequestTests（Seed→generationConfig）
- `NAI/NewLife.AI.sln` 构建 0 错误；全量测试 **1438 通过 / 0 失败 / 7 跳过**（较第七轮 +4 新用例）

### Phase 20（2026-08-04）— 第九轮协议专属能力参数修复（R9-1/R9-2/R9-3）
- **R9-1 Gemini 安全设置**：`GeminiRequest` 新增 `SafetySettings` 属性（序列化 `safetySettings`），`FromChatRequest` 经 `Items["SafetySettings"]` 透传 `[{category, threshold}]`
- **R9-2 DashScope 高分辨率参数**：`DashScopeParameters` 新增 `VlHighResolutionImages`/`MaxPixels`，`FromChatRequest` 经 `Items` 映射（键名与 `SendMessageRequest.Options` 文档一致）
- **R9-3 Bedrock 推理配置补全**：`BedrockInferenceConfig` 新增 `TopK`/`PresencePenalty`/`FrequencyPenalty` 并更新 `IsEmpty`，`FromChatRequest` 直映射（Converse API 原生字段 topK/presencePenalty/frequencyPenalty）
- 新增协议级测试 **3 用例**：GeminiRequestTests（SafetySettings 透传）、DashScopeRequestTests（VlHighResolutionImages/MaxPixels→Parameters）、BedrockChatClientTests（TopK/惩罚→inferenceConfig）
- `NAI/NewLife.AI.sln` 构建 0 错误；全量测试 **1441 通过 / 0 失败 / 7 跳过**（较第八轮 +3 新用例）

### Phase 21（2026-08-04）— 第十轮可用性参数修复 + 分析收敛（R10-1/R10-2）
- **R10-1 Ollama 上下文/驻留**：`OllamaOptions` 新增 `NumCtx`（上下文窗口大小），`OllamaChatRequest` 新增顶层 `KeepAlive`（模型驻留秒数），`FromChatRequest` 经 `Items["NumCtx"]`/`Items["KeepAlive"]` 透传，`hasOptions` 判断同步扩展
- **R10-2 Anthropic 用户标识**：`AnthropicRequest` 新增 `Metadata` 属性，`FromChatRequest` 将 `request.User` → `metadata.user_id`
- 新增协议级测试 **2 用例**：OllamaChatModelTests（NumCtx/KeepAlive 透传）、AnthropicRequestTests（User→metadata.user_id）
- `NAI/NewLife.AI.sln` 构建 0 错误；全量测试 **1443 通过 / 0 失败 / 7 跳过**（较第九轮 +2 新用例）

### Phase 22（2026-08-04）— 第十一轮响应侧一致性修复（R11-1/R11-2）
- **R11-1 流式聚合补全**：`FilteredChatClient.RunStreamingWithFiltersAsync` 聚合 `Delta.ReasoningContent` 与末 chunk `FinishReason`，`OnStreamCompletedAsync` 的 `context.Response` 现携带完整思考内容与结束原因（`resp.Add(content, reasoning, finishReason)`），学习/蒸馏过滤器可读取
- **R11-2 Text 多模态提取**：`ChatCompletionResponse.Text` 对多模态数组内容仅提取 `text` 类型片段拼接，不再返回数组 `ToString()` 垃圾值
- 新增协议级测试 **2 用例**：FilterTests（流式聚合响应含思考与结束原因）、ChatCompletionResponseTests（多模态数组 Text 提取）
- `NAI/NewLife.AI.sln` 构建 0 错误；全量测试 **1445 通过 / 0 失败 / 7 跳过**（较第十轮 +2 新用例）

### Phase 23（2026-08-04）— 第十二轮收尾审计修复（R12-1）
- **R12-1 Ollama 转换一致性**：`OllamaChatResponse.ToChatResponse()`/`ToStreamChunk()` 补齐与 `IChatResponse.Messages` getter 相同的映射（done 缺省 `Stop` + `tool_calls`→`FinishReason.ToolCalls`）
- 本轮同时完成收尾审计：能力接口实现一致性（Gemini `IImageClient`/`IModelListClient`、Ollama `IModelListClient`、DeepSeek `IBalanceClient`、DashScope/NewLifeAI `IRerankClient`、OpenAI 五接口均正确实现）、Ollama 流式终结帧（真实 API 带 message，ToolCalls 覆写可用）
- 新增协议级测试 **1 用例**：OllamaChatModelTests（ToChatResponse/ToStreamChunk 的 tool_calls→ToolCalls 断言）
- `NAI/NewLife.AI.sln` 构建 0 错误；全量测试 **1446 通过 / 0 失败 / 7 跳过**（较第十一轮 +1 新用例）

---

## 10. 残留风险

| 风险 | 说明 | 缓解 |
|------|------|------|
| Anthropic/Gemini/Bedrock 无真实 Key | 无法做真实网络验证 | mock 协议测试 + 请求序列化断言保障；集成测试待 Key 后可运行（如 DashScope/DeepSeek/Ollama 已真实回归） |
| OpenAI 原生 `enable_thinking` | OpenAI 官方不认识此参数（o 系列用 `reasoning_effort`），但不影响兼容服务商 | 保留（DashScope/Qwen 依赖），文档澄清；o 系列走 `reasoning_effort` |
| Bedrock think 映射依赖模型 | Converse thinking 行为随底座模型（Claude/Llama/Mistral/Nova）差异 | 已按官方 `additionalModelRequestFields` 文档实现并验证序列化；行为以官方文档为准 |
| 流式错误现在会显式抛出 | S3 修复后，服务商流式错误不再静默吞掉，可能暴露此前被掩盖的问题（如测试用错音色） | 属预期行为；消费方需对 `HttpRequestException` 做错误处理 |
| Gemini `ResponseFormat` 映射依赖模型 | `responseSchema` 需为 OpenAPI 子集；老模型可能不兼容 | 按官方文档实现；模型不兼容时服务商返回错误可感知，不会静默 |
| SDK 层重试默认关闭 | `RetryCount=0` 保持既有语义，需应用层显式开启 | 网关 `GatewayService` 已有自身 429 重试；Web/`MessageFlow` 按需在配置开启 |
| 流式重试仅限首字节前 | 流已开始（已收到首个 chunk）后不再重试，避免重复生成与重复计费 | 符合预期；流中断由断线续传机制兜底 |
| 多轮思考签名跨请求持久化 | Anthropic 思考签名仅在单次 `ToolChatClient` 运行的内存消息中保留；跨 HTTP 请求（应用层落库重放）需应用层持久化 `ChatMessage.Items["Signature"]` | SDK 层已完整支持工具轮次回传；应用层（宿主消息持久化）后续承接跨请求回放 |

---

## 11. 分析结论（收敛）

经过 **十二轮独立深入分析**（v1.0 → v4.8），`NewLife.AI` 协议客户端在 **chat / stream / think**（基础对话与增强对话能力）维度已覆盖完整，共修复 **45 个缺口**：

| 维度 | 覆盖内容 | 轮次 |
|------|----------|------|
| 流式健壮性 | SSE/NDJSON 解析、SSE 前缀统一、流式错误识别、畸形块容错、首字节前重试、流式用量合并 | R1/R2/R4/R6 |
| think 映射 | 6 协议 EnableThinking/ReasoningEffort 请求映射 + 响应思考提取、多轮思考回传（签名/redacted/adaptive） | R1/R3 |
| 用量明细 | reasoning/cache tokens 跨协议映射（含流式路径）、OpenAI max_completion_tokens | R5/R6 |
| 一致性盲区 | Gemini 流式错误、Bedrock 区域推导、多 system 合并、Items 透传、finishReason 补全 | R4/R6 |
| 多模态输入 | Anthropic/Gemini/Ollama/Bedrock 图片/音频输入转换 | R7 |
| 生成参数 | Ollama format/惩罚/NumCtx/KeepAlive、OpenAI/Gemini seed/logprobs、协议专属能力 | R8/R9/R10 |

**验证基线**：`NAI/NewLife.AI.sln` 构建 0 错误；全量测试 **1446 通过 / 0 失败 / 7 跳过**（跳过为未配置 Key 的集成测试）。六类协议（OpenAI 兼容 / Anthropic / Gemini / DashScope / Ollama / Bedrock）均有 stub 协议级测试覆盖。

**范围边界**：工具（Tool）相关能力（`ToolChatClient`、`IToolProvider`、工具循环、MCP 等）不在本文档范围，由独立分析另行覆盖。

**遗留说明**：本分析以「协议客户端 SDK 层」为边界；跨请求签名持久化、SSE 前端事件消费等属于应用层（宿主 `MessageFlow`）职责，SDK 层接口已就绪。

---

（完）
